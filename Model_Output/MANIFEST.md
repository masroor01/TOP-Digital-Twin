# Model_Output Manifest

**2026-09-01 per-MARKET models also tested head-to-head vs. the shared
model (Script 49, new) -- shared model wins even more decisively.**
Direct follow-up to Script 48 (state-restricted models lost 15/24 vs.
5/24 significant). Pushed the same question one level further: does a
model restricted to a single market's own data beat the shared model?
Tested on the 3 fullest-history markets per crop (504/504 weeks, no
coverage gaps -- all 9 are also the project's own Script-42-identified
"market leader" markets), including both Uttarakhand potato markets
specifically to follow up on Script 48's one genuine state-level win.
Same 5-fold rolling-origin CV/hyperparameters as the shared model's own
recorded backtest, DM-tested against identical (market, week) cells.
Result: shared model wins 9/36 comparisons significantly (p<0.05);
market-restricted wins **0/36**. Notably, Uttarakhand's state-level win
does NOT hold market-by-market: Kashipur APMC shows the same directional
pattern (lower MAPE restricted) but never reaches significance (only
~160-185 weeks of its own data -- not enough power), while Dehradoon APMC
is significantly WORSE restricted at short horizons. Read together with
Script 48: full pooling (shared model) beats partial pooling (state-
level, occasionally, with enough combined data) beats no pooling
(per-market, never observed to win here) -- the per-market idea is now
settled empirically, not just argued. Outputs:
`table_market_restricted_vs_shared_model_dm_test.csv`,
`market_restricted_model_predictions.csv` (audit trail).

**2026-08-31 per-state models tested head-to-head vs. the shared model
(Script 48, new) -- shared model wins.** Follow-up to the per-market/state
accuracy work above: a user argued that fitting one model per (crop, state)
would beat the current design (one shared model per crop+horizon, pooling
every market, with market_id/state as features) since it wouldn't be
"diluted" by other states. Tested empirically rather than argued: trained a
state-restricted M6 model for the TOP 2 highest-data states per crop
(tomato: Uttar Pradesh/Keralam; onion: Maharashtra/Kerala; potato: West
Bengal/Uttarakhand -- deliberately the best-case scenario for the per-state
argument), same 5 rolling-origin folds/hyperparameters as the shared
model's own recorded backtest, then ran a Diebold-Mariano test against the
shared model's predictions for the identical (market, week) cells. Result:
shared model wins 15/24 comparisons significantly (p<0.05), state-
restricted wins only 5/24 (mostly Uttarakhand and Keralam -- the two
smallest-data states in the sample, where the shared model's richer
training pool seems to help least, or Kerala/UP at longer horizons where
the state series may behave more idiosyncratically). Confirms the
bias-variance argument: even in the best-data-availability case, cutting
training data down to one state usually costs more (variance from thinner
data) than it gains (removing cross-state dilution). Outputs:
`table_state_vs_shared_model_dm_test.csv`, `state_model_predictions.csv`
(audit trail).

**2026-08-31 per-market accuracy added, then made hierarchical with
shrinkage (Script 47, revised same day).** The dashboard's "Model Accuracy"
KPI (100% - MAPE) is a crop+horizon-level statistic -- production models are
trained one per (crop, horizon) across ALL markets combined (Script 23), so
it's genuinely the SAME number for every market at a given crop/horizon, not
a bug. A user flagged this (reasonably) as looking wrong. Script 47 v1 added
a per-MARKET MAPE from actual per-market backtest predictions
(`dm_market_level_predictions.csv`, M6 variant -- the same file Script 18b/46
use), hiding cells with under 10 backtested weeks. The user then asked for a
more robust check, so v2 (same day) adds two things: (1) a STATE-level tier
(joined via `production_models/reference_rows.csv`'s crop/market_id->state
map), pooling every market in a state; (2) two-level empirical-Bayes
shrinkage -- market blends toward its own state's estimate, state blends
toward the crop-wide mean, each weighted by its own sample size (pseudo-
counts K_MARKET=52wk, K_STATE=100wk; see the script's docstring for the
exact formula). Every market/state now gets a number -- no hidden cells --
and thin-history cells get a trustworthy blended figure instead of either
noise or nothing. 6,788 (crop, market, horizon) cells, 194 state cells;
median 173 backtested weeks per market cell. Output:
`table_market_level_accuracy.csv` (now with raw + shrunk MAPE at both
tiers).

Tracks which script produced each output group, when it was last regenerated,
and whether it reflects the **current** data pipeline. Update the relevant
entry every time you re-run a script that writes here — this file exists
specifically because two stale-output bugs (`table_benchmarks.csv`,
`table_spike_auc.csv`) went unnoticed for weeks in July 2026 before being
caught during the 2026-07-29 results review.

**2026-08-31 directional accuracy test added (Script 46, new).** Every
metric used elsewhere in this project (RMSE, MAE, MAPE, R2, MASE) is
magnitude-based -- none of them ask whether the model called the right
DIRECTION of the price move. Script 46 closes this gap: per-market
(`market_id`-keyed), using the same `dm_market_level_predictions.csv`
Script 18b's DM tests are built on (M0/M6 only -- that's what exists in
this file), it looks up the origin price (horizon weeks before the target
week, from the raw weekly panel) and compares `sign(actual_change)` vs
`sign(predicted_change)`, with a binomial test against a 50% null per
(variant, crop, horizon) cell. All 24 cells are significant at p<0.05
given the large per-market sample sizes (17k-153k rows each). Headline
finding, cross-validating rather than contradicting the existing ablation
story: **onion and tomato improve or hold steady with horizon** (tomato
M6 hits 84% at h=13w; onion climbs from 55% at h=1w to 72% at h=13w), while
**potato's M0 pulls decisively ahead of M6 at long horizons** (h=26w: M0
78% vs M6 65%) -- the same "richer features don't help potato" pattern
the ablation study already found via MAPE/R2, now confirmed on an
entirely independent metric. B1_Naive's directional accuracy is exactly
0.0% in every cell by mathematical construction (it always predicts "no
change," so it never calls a direction) -- reported as a crop-level
context baseline only, not a fair per-market comparison. Outputs:
`table_directional_accuracy.csv`, `table_directional_accuracy_naive.csv`,
`fig_directional_accuracy.png`.

**Current pipeline state (as of 2026-08-29):** market panel filtered to
>=70% real coverage per market (**842 tomato / 813 onion / 82 potato**,
all three now landing on the **same week, 2026-08-24**, for the first time
— previously tomato/onion/potato each lagged behind at different real
cutoffs). Imputed rate on each crop's latest week is now genuinely low
(tomato 15.9%, onion 10.9%, potato 4.9% — potato specifically was 80.5%
imputed two weeks ago; see the 2026-08-21 and 2026-08-29 entries below for
the full story). Potato zones P1-P3 relocated to Darjeeling/Diamond
Harbour/Dehradun with real climate+satellite data. Any output dated before
2026-08-14 should be treated as stale relative to the current pipeline.

**2026-08-29 weekly-refresh automation built and stress-tested.**
`scripts/weekly_refresh/` (new) fully automates the data-pull-through-
retrain cycle: a genuine unattended scraper (`agmarknet_onion_prices.py`,
commodity-parameterized despite its name, hits `api.agmarknet.gov.in`
directly — no login/captcha) feeds `validate_scrape.py` (schema, price
sanity, market_id conflict, overlap-vs-trusted-file checks) then
`merge_scrape.py` (replaces the trusted file's current-year portion,
backing up first) then Scripts 09 -> 23 -> 44, orchestrated by
`run_weekly_refresh.ps1` and scheduled via Windows Task Scheduler
(`register_task.ps1`, Tuesdays 03:00). Building and live-testing this
found and fixed **two real bugs** before it ever ran unattended for real:
(1) a PowerShell function parameter named `$Args` collided with the
reserved automatic variable of the same name, silently splatting an EMPTY
argument list to `python` — this launched an interactive REPL instead of
the scraper, which then hung for **~46 hours** issuing console errors in
the non-interactive redirected context before being caught and killed;
(2) the same function's live log echo used `Write-Output`, which fed every
echoed line into the function's own return value, so `$code` became a
polluted array instead of a clean exit code and `if ($code -ne 0)` was
true almost regardless of the real result -- a genuinely successful
146,842-row tomato scrape got reported as FAILED and silently skipped
because of this. Both fixed, verified via isolated smoke tests, then
proven live through a full successful cycle. Also found: potato's price-
sanity check was mis-calibrated (see next entry) and a task-registration
script that printed "Registered" even when `Register-ScheduledTask`
actually failed with Access Denied under `-LogonType S4U` (no "Log on as a
batch job" right on this account) -- fixed to verify registration
independently and default to `-LogonType Interactive`. Full writeup in
`scripts/weekly_refresh/README.md`.

**2026-08-29 all-India potato scrape investigated, then scrapers scoped
down.** A live all-India potato scrape (done to test the automation above)
tripped `validate_scrape.py`'s price-sanity check: 9.6% of rows outside
[40, 3500] Rs/quintal. Investigated rather than just loosened the
tolerance blindly -- found this is NOT a data-quality problem: 97% of the
out-of-range rows are Tamil Nadu (median 0.22 tonnes/report, ~3800-4500
Rs/quintal) and Kerala. Tamil Nadu's "potato markets" turned out to be the
**Uzhavar Sandhai retail farmer-market scheme**, not wholesale APMC mandis
-- confirmed via arrivals volume (55x smaller than West Bengal's median 12
tonnes/report) and market naming. Kerala is a genuine but thin,
import-dependent consumption market. Neither is comparable to the
wholesale mandi data the rest of the panel is built from, and Script 09's
existing 8-year-balanced-panel + price-clip filters already exclude both
from the actual trained panel regardless (confirmed: still exactly 82
markets / 2 states after this refresh). `validate_scrape.py`'s price check
was changed from a hard 5% fail threshold to a WARN-at-5%/FAIL-at-20%
tier to reflect that this is expected non-panel-state variation, not
corruption. Given none of this ever reaches the model, the weekly
scraper's potato pull is now restricted to `--states
"West Bengal,Uttarakhand"` -- smaller, faster, and stops re-litigating a
non-issue every week. The trusted `potato_all_india_apmcs_2000_2026.csv`
will not gain further all-India history going forward as a result
(acceptable -- it never fed the model).

**2026-08-29 dashboard redesign found and fixed while reviewing
uncommitted work.** `scripts/24_Simulation_Dashboard.py` had a substantial
uncommitted UI/styling overhaul sitting locally since 2026-08-14 (new
font/color system, restyled labels, a `apply_react_chart_theme()` chart
helper) -- not produced by an assistant session, found while checking the
repo before this commit. Live-testing it in the browser preview surfaced a
crash: `apply_react_chart_theme()` set `titlefont` inside `xaxis`/`yaxis`
dicts, a Plotly property removed in current Plotly versions (`title` must
now be a nested dict with its own `font`) -- the dashboard failed on its
very first chart render. Fixed both occurrences; also fixed a minor cosmetic
issue found in the same pass (the "Target" star-marker trace had no
explicit hover text, leaking a literal "undefined" into the unified-hover
tooltip -- added a proper `hovertemplate`). Verified live in-browser after
the fix: dashboard renders fully, no console errors.

**2026-08-21 arrivals-outcome SDID robustness checks (new scripts).**
Script 31 Part C.4's arrivals/quantity ATT (onion arrivals fell relative
to synthetic control in every sub-window) had never been tested against a
null distribution, unlike the price-based ATT. Two new scripts close this
gap: `38b_SDID_Arrivals_InSpace_Placebo_Test.py` (in-space permutation,
169 donor markets, postban window) and
`39b_SDID_Arrivals_Stacked_MultiEpisode_EventStudy.py` (stacked design
across all 3 onion export bans, arrivals outcome). Results: the in-space
test **fails decisively** (p=0.680 — the real ATT sits well inside 169
markets' ordinary noise). The stacked test shows onion arrivals
separating from the tomato/potato placebo band in 10 of 13 post-ban
week-bins — but wrong-signed relative to the ban's stated mechanism
(should raise arrivals, not lower them) and already present in the
pre-ban escalation window (-25.8%, more negative than postban's -14.3%).
Read together: the arrivals decline is real (not market-level noise) but
corroborates the existing reverse-causality finding rather than
establishing an independent, ban-caused arrivals effect. See entries
below and the Script 31 entry for the original point estimates.

**2026-08-19/20 escalation-signature detector expansion (Script 40).** A
fact-check pass on the patent/disclosure materials found tomato and
potato's single episodes each had been evaluated in-sample only, while
onion (3 episodes even then) got genuine leave-one-episode-out validation
— an undisclosed confidence gap. Fixed by researching and primary-source-
verifying three further episodes (tomato Jul-2024 PIB PRID=2038421,
tomato Aug-2025 PIB PRID=2154078, potato Jul-2016 DGFT Notification 15),
bringing the design to a genuine 8-episode, fully held-out test across all
three crops (onion 2019/2020/2023, tomato 2023/2024/2025, potato
2014/2016 — the last two potato episodes draw on the separate longhistory
panel, since they predate the main panel's 2017 start). Two hardcoded-by-
crop-name bugs (the placebo `score_type` label and the case-study figure
grid, both keyed to the literal string `'onion'`) were found and fixed in
the same pass — both would otherwise have kept the new episodes mislabelled
`in_sample` even after being added. Honest result: mean held-out AUC
0.801 tomato / 0.831 onion / 0.849 potato; 5 of 8 episodes reach placebo
significance at p<0.05 (both potato, 2 of 3 onion, 1 of 3 tomato) — see
the updated table row below; this supersedes the 2026-08-08 "onion
2019/2023 clean, 2020 not" partial summary previously in this file.

**2026-08-15 market-level DM test re-run (Script 18b) — closes a gap this
file previously listed as deferred.** Script 15's `MARKET_LEVEL_DIAGNOSTIC`
branch was patched to carry `market_id` through to
`dm_market_level_predictions.csv` (it previously carried only the market
name, which is not always unique — see the 2026-08-14 entry below), then
re-run, then Script 18b re-run against the corrected predictions. No
longer 🟡 stale as this file previously said — see the updated table row.

**2026-08-14 market-name-vs-`market_id` collision bug, project-wide.** A
handful of market NAMES repeat across different STATES within the same
crop (Fatehabad APMC in Haryana AND Uttar Pradesh for both tomato and
onion; Balugaon APMC in Odisha AND Assam for tomato; Pratapgarh APMC in
Rajasthan AND Uttar Pradesh for onion) — 4 true `(crop, market)`
collisions out of 800+ markets per crop. Any script that grouped or
pivoted on the market NAME column rather than `market_id` silently merged
these physically distinct markets, corrupting lag/rolling features and
target construction for the affected markets specifically. Found via a
market-count audit, fixed across Scripts 15, 15b, 15c, 23, 25, 29, 31, 33,
42 by switching the join/group key to `market_id` (or, for 31/42's many
pivot call-sites, relabelling colliding names to include state once at
load time). **Two more scripts were found still vulnerable on 2026-08-20
and separately fixed and re-run: 38 (in-space placebo) and 39 (stacked
multi-episode event study)** — both had been missed in the original nine-
script pass. Consistent with every other instance of this bug in the
project, re-running 38/39 with the fix produced results **bit-for-bit
identical** to the pre-fix versions — a confirmed, not assumed, null
effect on those two analyses specifically. **Scripts 35/36 (the rejected
two-phase architecture) were ALSO fixed in this pass** (commit `cacdea5`,
same day as 38/39) — both group on `market_id` throughout, confirmed by
reading the current code; this was missed in an earlier write-up of this
section, corrected 2026-09-13. Script 34 (the panel-join stage) never had
the bug to begin with — it does no per-market feature engineering. Low
priority regardless, since the architecture is already rejected on its
own merits, but no longer an open collision-fix item.

**2026-08-04 decision: the 23-year two-phase residual architecture is
REJECTED, not "deferred pending a decision."** Script 33's real-but-modest
~1.2pp average MAPE improvement from a longer training window (noted
below) was built out into a full two-phase baseline+residual stack
(Scripts 34-37) and formally tested. Result: the combined Phase1+residual
forecast is worse than both Phase 1 alone and production M6 at nearly
every crop/horizon cell; Phase 1 alone's apparent edge over M6 (notably
onion) does not survive formal DM testing (1/12 cells significant at
p<0.05). M6 remains the sole production/paper architecture — see the
"Exploratory" table below for the full Script 32-37 breakdown.

**2026-08-04 look-ahead leakage fix.** `s2_ndvi_anom`'s seasonal
climatology (Script 14) was a full-sample mean per (zone, ISO-week) — a
2018 row's anomaly could use NDVI observed as late as 2026. Fixed to an
expanding mean over strictly prior ISO-years only (each zone's first S2
year, 2017, now has genuine NaN anomaly instead of a leaked value;
overall `s2_ndvi_anom` coverage drops 35.0%→31.1%, consistent with losing
exactly one year per zone). Full downstream cascade retrained against the
corrected feature: Scripts 15, 23, 25, 27, 28, 30 (NOT 12/13/17/18b/26,
NOT 29/31/38/39 — see their individual entries for exactly which date's
run they reflect). Net accuracy impact small and mixed, as expected for a
smooth seasonal term (e.g. onion h=13w MAPE 40.9%→35.8%); no architectural
or ranking changes.

**2026-08-01 policy-events verification + `PANEL_END` fix.** The
dashboard's "carried forward, ~30w stale" warning on every export-policy
control was traced to `PANEL_END` being frozen at 2025-12-31 while the
price panel had already grown past it — not an inherent external-data lag
like macro/climate, as previously assumed. Extended `PANEL_END` to
2026-12-31, re-ran Scripts 19→22→23. Also verified and added 5 new 2026
policy events to `TOP_policy_trade_verified_2017_2026.xlsx` after
cross-checking a user-supplied AI-drafted policy report against
independent primary sources rather than trusting it at face value (one
claim included a district-level figure that did not match independent
reporting and was deliberately left out rather than included on
convenience). See Script 19's entry below.

**2026-08-01 WPI vintage refresh.** Discovered while building an unrelated
long-history data audit (`scripts/10c_RBI_LongHistory_Parser.py`) that CMIE
restates the *entire* history of its item-level WPI series (and PPAC
diesel/LPG) on every pull, not just the newest months — confirmed with the
project owner, a CMIE subscriber. `data/rbi_dbie/rbi_dbie_macro_2017_2025.csv`
and `data/ppac_macro/ppac_diesel_lpg_2017_2025.csv` were built at an earlier
pull and had drifted from CMIE's current vintage across their whole window:
mean |diff| 13.7% for `wpi_tomato`, 9.4% `wpi_onion`, 6.9% `wpi_potato`, 6.6%
`wpi_vegetables_total`, 3.8% `wpi_fruits_vegetables`, 1-3% diesel/LPG (repo
rate/reverse-repo/USD-INR unaffected — discrete/point-in-time, don't get
restated). Refreshed both files to the current vintage, re-ran Script 22
(master join) and Script 23 (retrain). Measured impact: `model_uncertainty.json`
(validated RMSE/MAPE per crop/horizon) came out byte-identical despite tree
counts shifting per model — a real but small effect, not headline-moving.
`reference_rows.csv` (dashboard's live baseline) unchanged, since only
historical training rows were affected, not the latest week. Not treated as
grounds for a full ablation/crisis-backtest/SHAP/stress-test cascade re-run
on this basis alone. If Script 15/25/28/30/31 get re-run for some other
reason, they will pick up the refreshed vintage automatically (no separate
action needed there).

**2026-08-03 macro long-history extension.** Built to feed the two-phase
architecture's 2003-2026 baseline panel (`scripts/10c_RBI_LongHistory_Parser.py`,
extends repo/reverse-repo/USD-INR/WPI/diesel-LPG back to their real source
floors) and to extend `scripts/10_CMIE_Macro_Parser.py`'s own window from
2017-01-01/2025-12-31 to 2000-01-01/2026-12-31. As a side effect this fixed
a pre-existing gap: `export_veg_usd_mn`, `import_veg_usd_mn`, and
`crude_oil_usd_bbl` were NaN for early-2026 rows in production even though
the already-on-disk source files had that data — just not previously
re-parsed. Validated at 0% drift vs. production across all 6 re-parsed
columns before trusting the extension. Two pre-existing mislabeling
quirks (`agri_wages_rs_day` is all-occupations not agri-specific;
`iip_food_proc` is overall manufacturing not food-processing) were left
untouched, same as always — see README §9.

**2026-08-01 grid-adaptivity fix — read this before citing any market
count from before this date.** Script 09's weekly grid was built from one
fixed global `START_DATE` for every market (a plain cartesian product of
markets x weeks), so a market that only began reporting years after
`START_DATE` was scored as "missing" for every week before it existed in
the system — silently dragging its real-coverage ratio down for a reason
that had nothing to do with its actual data quality. Discovered while
investigating whether extending the panel's history back to 2003 was
worthwhile (see `scripts/32_LongHistory_Panel_Builder.py` /
`scripts/33_LongHistory_Validation_Experiment.py`, an exploratory side
study, not part of the main pipeline): at a 23-year window this collapsed
the long-history panel to 162/98/28 qualifying markets; fixing the grid to
start each market at its own first real observation recovered it to
684/746/72. The SAME bug existed at the *production* 9-year (2017-2026)
window too, just with milder consequences — fixing it there and re-running
Script 09 changed the production panel from **517 -> 834 tomato** and
**246 -> 809 onion** markets (potato: 82 -> 82, unchanged, because its
separate >=8-years balanced-panel prefilter already guaranteed every
candidate market existed for nearly the whole window, leaving nothing for
the bug to affect). Median real coverage of the newly-recovered markets is
92-96%, not thin/borderline data. (The manuscript's own Data & Study Area
section documents this in full; `paper_drafts/` is a working folder that
gets cleared between manuscript-writing passes, so it is not a stable
citation target from this file — cite the live panel or this manifest
instead.) Given the scale of the change, the entire downstream cascade (ablation,
production models, SHAP, MCS, crisis backtests, Granger causality, stress
test, SDID) was re-run the same day — see individual entries below.
Deferred: Scripts 15b/15c (tree/LSTM model-family comparisons, ~140 min
combined, unlikely to qualitatively change) and 18b (market-level DM,
needs the `MARKET_LEVEL_DIAGNOSTIC` toggle round-trip) — both still
reflect the pre-fix, smaller panel; treat as directionally valid but
numerically stale until re-run.

Status legend: 🟢 current · 🟡 stale, known, re-run pending · ⚫ deprecated/superseded · ⚪ orphaned (no current script produces it)

| Script | Outputs | Last generated | Status | Notes |
|---|---|---|---|---|
| `09_Agmarknet_Weekly_Panel.py` | `data/agmarknet_weekly/*.csv` (not in Model_Output) | 2026-08-29 | 🟢 | Grid-adaptivity fix (see 2026-08-01 note below). `END_DATE` now defaults to today automatically (`TOP_DT_END_DATE` env var to override) — added for the weekly-refresh automation, see 2026-08-29 entry above; safe by construction since each crop's grid still caps at its own real max date. Current market count 842 tomato / 813 onion / 82 potato, all three now on the same latest week (2026-08-24) for the first time. 70% real-coverage filter, 2017-2026 window. |
| `11_Market_Selection_And_DataStructure.py` | `filtered_panel_top.csv`, `appendix_market_selection.xlsx`, `fig01-05_*.png` | 2026-09-02 (re-run) | 🟢 | Re-run after a real market_id-vs-name collision fix in `compute_coverage()` (independent audit finding — coverage was grouped by market name, silently blending same-named markets across states into one coverage figure). Current selection: tomato 836/839 (100%), onion 803/809 (99%), potato 82/82 (100%) at ≥70% coverage. |
| `12_ModuleB_RollingOrigin_MultiHorizon.py` | `table_rolling_origin_metrics.csv`, `table_spike_auc.csv`, `fig_horizon_r2.png`, `fig_rolling_origin_rmse.png`, `fig_spike_roc.png`, `fig_mape_by_horizon.png` | 2026-07-29 | 🟡 | Predates the 2026-08-01 grid fix — market set has since grown substantially (esp. onion, 246->809). Own 3-fold structure (test years 2022-2024), independent of Script 15. Re-run pending. |
| `13_Benchmark_Models.py` | `table_benchmarks.csv`, `table_comparison.csv`, `fig_benchmark_comparison.png`, `fig_skill_score.png`, `fig_r2_comparison_heatmap.png` | 2026-09-02 (re-run) | 🟢 | Re-run on the current panel with the market_id collision fix (165846e) and the new national-level B4_ARIMA h=4/13/26 variants (e9c967f, closing the docstring gap where only h=1 per-market was ever implemented). **B1_Naive here is still not the ablation study's naive baseline** — use Script 15's inline B1_Naive for any M0-M6 comparison. **National ARIMA h=4/13/26 result includes one genuine, diagnosed outlier**: tomato fold 3 (test year 2024, train ending 2023-06-30) shows MAPE=1013.9%/R²=-453.8 at h=4 — traced directly to real data, not a bug: training ends mid-spike (tomato's real Jul-2023 price escalation, prices rising 992→1144→1445→2012→4467 Rs/quintal in the training window's final 5 weeks), and ARIMA(1,1,1)'s AR component extrapolates that momentum without mean-reversion, diverging to an implausible ~₹25,825/quintal steady-state forecast. This single fold dominates the crop-level MEAN (tomato mean MAPE h=4: 392.7%) — the MEDIAN across folds (130.1%) is far more representative and should be preferred when citing a single tomato ARIMA number. Onion/potato show no comparable outlier (mean and median are close). |
| `14_Satellite_Climate_Features.py` | `fig_era5_temperature.png`, `fig_chirps_rainfall_heatmap.png`, `fig_s2_ndvi_anomaly.png`, `fig_satellite_cross_validation.png`, `fig_climate_satellite_correlation.png`, `data/satellite_climate/*.csv` | 2026-08-04 | 🟢 | Crop/zone-level, not market-level — unaffected by the market-panel grid fix. Includes real ERA5/CHIRPS/S2/MODIS for the relocated potato zones. Re-run 2026-08-04 for the `s2_ndvi_anom` look-ahead leakage fix (see note above) — coverage drops 35.0%→31.1% as expected. |
| `15_Ablation_Study_M0_M4.py` | `ablation_raw_results.csv`, `ablation_predictions.csv`, `table_ablation.csv`, `table_mase.csv`, `fig_ablation_*.png` | 2026-09-03 (re-run) | 🟢 | Re-run to pick up the 2026-09-02 fix (165846e: extends forward-fill from wage/road_density to MACRO_COLS/CLIMATE_FEATS/SAT_FEATS, which previously silently `fillna(0)`'d the back half of Fold 5's 2026 test window once those sources ran out of real coverage). 420 LightGBM fits, 22.2 min, clean run (no errors/NaNs). **Real effect, concentrated in Fold 4/5 (2025/2026) and h=26w** — 132/480 rows moved by >1 MAPE point (max: tomato M0 h=26w fold1 51.1%→67.2%); earlier folds/horizons mostly stable. **Headline crop/horizon pattern unchanged**: potato's mean M6 MAPE still drops from 13w (45.6%) to 26w (43.4%) — the "accuracy increases at longer horizon" finding survives the fix, confirming the dashboard's Feature Importance tab explanation is not an artifact of the pre-fix data. Pre-fix outputs backed up to a local scratch dir before this run, not committed. `MARKET_LEVEL_DIAGNOSTIC` left at its resting state (`False`) for this crop-level pass. **Follow-up diagnostic pass run same day**: flag flipped to `True` (M0/M6 only, 2 of 7 variants, 15.9 min), regenerating `dm_market_level_predictions.csv` (2,079,906 rows, `market_id` correctly carried through) and `dm_market_level_raw_results.csv`, then flag reverted to `False` before committing — resting state confirmed unchanged in the committed file. This refresh cascaded through Scripts 18b/23/46/47 (all re-run same day, see their rows) and Script 50 (re-run, comparison-only, no production metric changed). The potato Jul-Oct-vs-Feb-Apr seasonal split backing the dashboard's Feature Importance/Multi-Horizon tooltip was independently re-verified against this fresh file and held within ~1 point of the figures already live — no tooltip correction needed. **Re-run again 2026-09-07** (both the full ablation and a follow-up `MARKET_LEVEL_DIAGNOSTIC` pass, 29.4 min + 12.1 min) to propagate the 2026-09-03/07 CMIE macro extension (agri wages, repo/reverse-repo, bank credit, diesel). Clean, no errors. **M0 (price-only) unchanged, as expected** — it never touches macro columns. **M2 (+Macro) also came back exactly unchanged (0.0pt diff on every one of 60 rows)** — a genuinely unexpected result worth flagging rather than quietly accepting: it implies the CV folds' actual training/test date windows don't extend far enough into the newly-added Jun-Aug 2026 macro months for the extension to be visible to any fold, though this hasn't been traced to a specific line of code and should be checked before relying on it. Feeds directly into Script 23's same-day full retrain (see that row). **Re-run again 2026-09-10, extended M0→M7** (added M7 = + Drought: VEDAS Trigger-1 + IDM CDI, joined on (state, district, week_start) with explicit `trigger1_missing`/`cdi_missing` flags rather than forward-fill, since M7's gaps are core-coverage — whole seasons, whole pre-2021/2022 years — not a tail gap like M5/M6's wage/road series; forward-filling would have fabricated a "no drought"/"normal CDI" reading for periods the source never covered). 384 LightGBM fits (8 variants x 4 folds x 4 horizons x 3 crops), 2512.9 min elapsed (background run spanning a multi-day session gap; actual CPU time ~confirmed healthy throughout via periodic checks, not a hang). **Honest result: M7 vs M6 is a mixed, small effect, not a clear win** — improves R² at 8 of 12 crop×horizon cells (all differences <0.04 R², well within the noise band already seen between M5/M6), degrades it at the other 4 (notably tomato h=4w: 0.312→0.276, and h=26w: -0.153→-0.178). Potato shows the most consistent improvement (h=13w: -2.253→-1.830, h=26w: -0.924→-0.870 — still deeply negative in absolute terms, but the least-bad of any variant at long horizons), plausibly reflecting potato's near-100% IDM CDI volume coverage (West-Bengal-dominated) found during Script 53/54's own validation. ~~No formal significance test (DM-style) run on M7 vs M6 yet~~ — **done 2026-09-13, see Script 55's row below.** The raw-metric read above is **partly overturned** by that formal test: potato h=13w's headline +0.30 R2 gain does NOT hold up market-by-market (2.4% significant, split 1-1), while onion h=1w and potato h=1w — cells this raw table showed as nearly flat (+0.003/+0.006 R2) — turn out to have real, broad, statistically significant improvements the crop-level average was hiding. **Re-run again 2026-09-11** (58.0 min, clean) to propagate a Script 54 bugfix found during a full VEDAS/IDM cross-check: VEDAS carries two internal polygons both named "PURBA MEDINIPUR" for West Bengal, disagreeing on 7/42 Kharif fortnights in the 2022 season; `drop_duplicates(keep='first')` had silently picked whichever came first in VEDAS's JSON key order, resolving all 7 to "No". Fixed to inclusive-OR (Yes if either sub-polygon triggered) — the district (`Medinipur(E)` in the panel, 127,333 tonnes of tomato/onion/potato arrivals) flips to "Yes" for Jul 18-Oct 17 2022. **Only potato's long-horizon M7 numbers moved**: h=13w -1.9518 (was -1.8302), h=26w -0.8687 (was -0.8700, negligible) — every other cell (including all of M6, which never touches drought features) is byte-identical, confirming the fix's blast radius matches its actual scope (one district, one season). Net effect: potato's h=13w M7-vs-M6 improvement *shrinks* from +0.4228 to +0.3012 R² — still the layer's strongest positive result, just smaller and more honestly computed than the pre-fix number. The cross-check also surfaced a separate, more serious **pre-existing** bug: `geocode_cache.json` (shared with Script 16) has wrong coordinates for Tamil Nadu's Thirunelveli/Thirupur (~13°N, near Chennai, instead of their real ~8.7°N/~11.1°N locations), which Script 16 confirmed sends 13 real tomato/onion markets to Andhra Pradesh zones 189-395km away for their M3/M4 features. Not fixed — out of scope for the drought layer, flagged as an open item below. |
| `15b_Tree_Model_Comparison.py` | `table_tree_model_comparison*.csv`, `fig_tree_model_comparison.png` | 2026-07-29 | 🟡 | Predates the 2026-08-01 grid fix. ~115 min re-run, deliberately deferred — qualitative conclusion (RandomForest worst, LightGBM/XGBoost/CatBoost competitive) unlikely to change with a larger panel. |
| `15c_LSTM_Transformer_Comparison.py` | `table_lstm_transformer_comparison*.csv`, `fig_lstm_transformer_comparison.png` | 2026-07-29 | 🟡 | Predates the 2026-08-01 grid fix. ~23 min re-run, deliberately deferred alongside 15b. |
| `16_Zone_Assignment.py` | `fig_zone_assignment_*.png`, `data/zone_assignment.csv` | 2026-09-02 (re-run) | 🟢 | Re-run with a new `MAX_ZONE_DISTANCE_KM=500` cap (a market whose nearest same-crop zone exceeds this is now left `zone_id=None` rather than assigned a climatically-unrelated proxy) plus geocoding retry-vs-permanent-failure handling. **The cap's real impact is larger than the introducing commit's own dry-run estimate** (that commit's comment said "113/839 markets (13%)" — apparently tomato-only; the actual current run shows tomato 246/842 (29%) and **onion 564/813 (69%)** unassigned, potato 0/82 unaffected — worth knowing if this file is ever relied on more heavily). **No downstream impact today**: `market_zone_features.csv` (the file this cap actually affects) is generated by Script 14 but not consumed by any other script in the pipeline — the production climate/satellite features (`crop_weekly_features.csv`, used by Scripts 15/23/40/41) are crop-level, not zone/market-level, so this finding is informational, not a production regression. **Re-run 2026-09-11** to fix a `geocode_cache.json` bug found during the M7 drought-layer cross-check: Nominatim's top match for the misspelled panel district names "Thirunelveli" and "Thirupur" (OSM's own admin boundaries use "Tirunelveli"/"Tiruppur") was a bakery shop and a park in Chennai's Tambaram suburb respectively — not the districts at all — so both were cached at ~13°N/80°E instead of their real locations (~8.5°N/77.6°E and ~10.8°N/77.5°E). Corrected via the properly-spelled Nominatim query, re-cached under the panel's original (misspelled) keys so Script 16 needed no code change. **Confirmed no impact on any trained model**: with correct coordinates, 3 of the 4 affected crop×district cells now correctly exceed the 500km cap and drop to `zone_id=None` (previously passed the cap on a falsely-short 189-395km distance to Andhra Pradesh zones); the one that still resolves (Thirupur/tomato, 269km to Karnataka's T1_Kolar) is informational only, since `market_zone_features.csv` has no downstream consumer per the note above. |
| `17_TFT_Model.py` | `tft_raw_results.csv`, `table_tft_vs_lgbm.csv`, `fig_tft_*.png` | 2026-07-22 | 🟡 | Reduced-scope run only — full-capacity run remains deferred (see README §9), now additionally predates the 2026-08-01 grid fix too. |
| `18_Diebold_Mariano_Tests.py` | `table_diebold_mariano.csv`, `fig_dm_pvalues.png` | 2026-09-03 (re-run) | 🟢 | **Re-run against the 2026-09-03 Script 15 refresh** (it reads `ablation_predictions.csv`, which that re-run regenerated). **Headline (M0 vs M6) now 2/12 significant, was 3/12**: tomato h=26w **lost significance** (was p=0.012 "M6 better", now p=0.102, same direction but no longer significant — a real conclusion change caused by the forward-fill fix, not noise) while potato h=13w/h=26w remain significant with M0 (price-only) better (p=0.031/0.048, was 0.023/0.025 — same conclusion, p-values shifted). If tomato's 26w M6-vs-M0 result is cited anywhere as significant (paper draft, slides), that citation is now stale and should be corrected to "not significant, same direction." Secondary (M0 vs M4): 3/12 significant (was 2/12) — also changed, not yet cross-checked cell-by-cell. |
| `18b_Market_Level_DM_Check.py` | `table_dm_market_level_*.csv` | 2026-09-03 (re-run) | 🟢 | Re-run against the 2026-09-03 `dm_market_level_predictions.csv` refresh (Script 15's `MARKET_LEVEL_DIAGNOSTIC` pass, picking up the 2026-09-02 forward-fill fix — see Script 15's row). M0-vs-M6 per-market, current figures: h=1w significant markets favour M0 for tomato/onion/potato (71.9%/63.1%/75.0%); h=13w flips toward M6 for tomato/onion (62.5%/87.5%) but stays exclusively M0 for potato (100%, 30/30). **Potato h=1w is a real reversal from the last-recorded figures** (previously read as M6-favoured 75.0% at h=1w — now M0-favoured 75.0%, the exact opposite) — treat any earlier potato h=1w citation as superseded. Tomato/onion h=1w/h=13w directions match earlier notes, magnitudes shifted (e.g. tomato h=13w M6-favoured share was recorded as 96.4%, now 62.5% — same direction, much less lopsided). |
| `19_Policy_Trade_Events.py` | `data/policy_trade/policy_weekly_features.csv` (not in Model_Output) | 2026-08-01 | 🟢 | `PANEL_END` extended 2025-12-31→2026-12-31 (see note above) — fixes the dashboard's ~30-week-stale policy-control warning. 5 new 2026 events verified against primary sources and added (CBIC notification, PIB release, 3 press-corroborated buffer-price hikes, sourcing tier kept honest for the latter). |
| `20_Labour_Wages_Layer5.py` | `data/labour_wages/wage_agri_state_monthly.csv` (not in Model_Output) | 2026-09-02 (re-run) | 🟢 | **Fixed 2026-09-02: `PANEL_END` was still hardcoded to `2025-12-31`, silently truncating every 2026 wage row before the join.** Note this was flagged as fixed by an earlier same-day commit titled "Fix PANEL_END truncation" — that commit only widened Script 22's own `PANEL_END`; this script's separate bound was untouched and the bug was still live. Widened to the `2030-12-31` generous sanity-guard convention used in Scripts 09/22/23. Re-run: output grew 2,782 → **2,860 rows** (78 previously-truncated 2026 monthly rows now included), 73.1% direct state matches, rest documented proxies. |
| `21_Infrastructure_Layer5.py` | `data/infrastructure/cold_storage_by_state.csv`, `road_density_state_annual.csv` (not in Model_Output) | 2026-09-02 (re-run) | 🟢 | **Fixed 2026-09-02, same class as Script 20 and also still live despite the same "PANEL_END truncation" commit**: `PANEL_YEARS = range(2017, 2026)` covers 2017-2025 only (Python range excludes the upper bound), so any 2026 week's `(state, year)` join got **zero** row match — not even a forward-filled one. Widened to `range(2017, 2031)`. Re-run: road-density output now 364 rows (26 states x 14 years, was 26 x 9); 286/364 forward-filled from the last measured year (CEIC series genuinely ends FY2019-20). Cold storage unchanged (static, no year dimension). |
| `22_Master_Panel_Join.py` | `data/master_weekly_panel_all_layers.csv` (gitignored, 696MB — regenerate, don't commit) | 2026-09-10 (re-run, M7 added) | 🟢 | Re-run after the Script 20/21 truncation fixes above and its own `PANEL_END` widening (2030-12-31). All 6 layer joins verified fan-out-free (522,082 rows in → 522,082 out at every step, via the script's own `checked_merge` assertions). Missing-value rates per layer: macro 7.6%, climate/satellite 13.8%, wages 13.3%, cold storage 0%, roads 0%, policy 0%. Final: 522,082 rows x 62 columns. **2026-09-10: added M7 (drought — VEDAS Trigger-1 + IDM CDI, see Scripts 51-54), join key (state, district, week_start), both joins fan-out-free.** M7 missing-value rate 82.5% of rows have at least one missing M7 value — expected and by design (Trigger-1: Kharif weeks 2022+, crosswalked districts only; IDM CDI: 2021-07-14 onward only), not a join defect. Final: 522,082 rows x 66 columns. **Note: this consolidated file is NOT what Script 15 (ablation study) actually trains on** — Script 15 loads and joins every layer itself from the same underlying per-layer files; this file is for the eventual TFT run and direct exploration. Keep both scripts' join logic in sync by hand when a layer changes. |
| `23_Train_Production_Models.py` | `production_models/` (subfolder) | 2026-09-07 (full retrain); `model_uncertainty.json` refreshed 2026-09-03 | 🟢 | **Full retrain 2026-09-07** to propagate the 2026-09-03/07 CMIE macro extension (agri wages, repo/reverse-repo, bank credit to agriculture, diesel — all extended to mid-2026, see `10b_Extend_Macro_2026.py`'s docstring) into the actually-deployed models, not just the ablation study. All 12 models retrained on the full history (panel joined: 522,082 rows x 69 columns); `model_uncertainty.json` values unchanged from the 2026-09-03 refresh (RMSE/WAPE sourced from files the macro extension didn't move — see Script 15's row). JS conversion (`generate_js_models.py`) and cross-language verification (`verify.mjs`) both re-run: **all 12 models bit-for-bit identical between Python and JS** (max_diff=0.000e+0 on every model). Bundled `web/data/production_models/` and `web/backend/src/models/*.js` synced; verified live (tomato h=4w Model Accuracy correctly shows ~65%, matching `model_uncertainty.json`'s tomato_4w WAPE of 35.3%). Note: the ablation study's own M2 (+Macro) variant showed **zero** measurable change from the macro extension (Section retained for the record — the CV folds' training/test windows apparently don't reach far enough into the newly-extended Jun-Aug 2026 range for the extension to move fold-level metrics; worth a closer look if this surprises a future reader, not yet fully explained). Below is the original 2026-08-01 through 2026-09-03 history, retained for continuity: retrained on the grid-fixed panel — 1,721 market baselines (up from ~845). Monotonic constraint on the three export-control policy features retained; oversampling attempt tried and reverted — magnitude question now handled by Script 31 (SDID) instead of asked of this model. Retrained 2026-08-01 for the WPI vintage refresh (tree counts shifted per crop/horizon but validated RMSE/MAPE came out byte-identical) and **again 2026-08-04 for the `s2_ndvi_anom` leakage fix** — small, real, non-headline-moving effect each time; `reference_rows.csv` (dashboard's live baseline) unchanged both times, since only historical training rows were affected, not the latest week. **`model_uncertainty.json` switched from MAPE to WAPE 2026-09-02** (see Script 47/50) — the actual trained models are untouched by this (the metric doesn't feed training), so only the JSON was regenerated, not a full retrain; computed from `dm_market_level_predictions.csv`'s M6 rows (pooled, ratio-of-sums) rather than averaging Script 15's fold-level MAPE scalars from `ablation_raw_results.csv` (RMSE still sourced from there, unaffected by the MAPE/WAPE issue). **Regenerated again 2026-09-03** to pick up the 2026-09-02 forward-fill fix (both RMSE's source `ablation_raw_results.csv` and WAPE's source `dm_market_level_predictions.csv` were stale relative to it until today) — all 12 cells moved by ≤2.3 WAPE points, no headline reversal (potato still less accurate at 13w than 26w: 42.1% vs 35.7% WAPE). Synced to `web/data/production_models/model_uncertainty.json` and verified live. |
| `25_Horizon_SHAP_Analysis.py` | `table_shap_by_layer.csv`, `table_shap_top_features.csv`, `fig_shap_*.png` (layer_composition, top_features, beeswarm_onion_4w) | 2026-08-04 | 🟢 | Re-run against the retrained production models, most recently for the `s2_ndvi_anom` leakage fix. |
| `26_Weekly_To_Daily_Disaggregation.py` | `table_dow_pattern.csv`, `table_disagg_backtest.csv`, `fig_disagg_example.png`, `fig_dow_pattern.png` | 2026-07-30 | 🟡 | Predates the 2026-08-01 grid fix AND every fix since (policy/PANEL_END, WPI vintage, macro long-history, `s2_ndvi_anom` leakage). Not yet re-run; the day-of-week pattern and noise-band conclusions are unlikely to shift much but the reference series should be refreshed. |
| `27_Horizon_Skill_And_MCS.py` | `table_horizon_skill.csv`, `table_horizon_skill_crossover.csv`, `fig_horizon_skill.png`, `table_mcs.csv`, `table_mcs_membership.csv`, `fig_mcs_membership.png` | 2026-09-07 (re-run) | 🟢 | **Re-run to re-establish a current baseline** after an external technical review (dated Sept 2026, addressed to Prof. Faaiz) was validated against the repo and found to accurately describe the project's state as of the 2026-08-06 commit — since superseded by the 5-fold CV extension, market_id collision fix, WAPE switch, and the 2026-09 CMIE macro extension, all of which changed `ablation_predictions.csv`/`table_mase.csv` this script reads. Crossover horizons **unchanged** from the Aug-4 run (tomato 4w, onion 13w, potato 26w) — a genuinely stable finding across two different codebase states a month apart. MCS pattern also stable: potato's B1_Naive still survives (never eliminated, p=1.000) at h=13w in both the Aug-4 and today's run — this qualifies the external review's generalized claim that "M6 dominates at h=13/26w": true in aggregate/for tomato+onion, not true for potato specifically, where naive remains statistically undefeated at h=13w even now. |
| `28_Crisis_Backtesting_Case_Studies.py` | `table_crisis_backtests.csv`, `fig_crisis_tomato_2023.png`, `fig_crisis_onion_2023_24.png`, `fig_crisis_potato_2024_25_spike.png`, `fig_crisis_potato_2024_25_crash.png` | 2026-09-07 (re-run) | 🟢 | **Re-run for the same reason as Script 27's row above.** Per-cell MAPE values shifted slightly (e.g. tomato h=13w crisis MAPE 26.5%->17.8%) but the win/loss pattern is **identical** to the Aug-4 run: M6 beats naive in exactly 8/16 crisis-window comparisons, same 8 cells both times. Naive still wins at h=1w in all 4 episodes; potato's Apr-2024 spike is still the one episode where M6 loses to naive at every horizon. A genuinely robust finding, not an artifact of the pre-fix data. |
| `29_Granger_Causality_Analysis.py` | `table_granger_layers.csv`, `fig_granger_layers.png`, `table_granger_market_network.csv`, `fig_granger_market_network.png` | 2026-08-01 | 🟡 | Re-run on the grid-fixed panel; predates the WPI-vintage/macro-long-history/leakage fixes since. Conclusions unlikely to shift (Granger significance is robust to small feature drift) but not re-verified. Its own top-5-by-arrivals market network is now superseded by Script 42's top-20 net-leadership analysis (see below) — Script 42 is the current reference for market lead-lag, not this file's `table_granger_market_network.csv`. |
| `30_Formal_Stress_Testing.py` | `table_stress_test_results.csv`, `fig_stress_test_{tomato,onion,potato}.png` | 2026-08-04 | 🟢 | Re-run on the grid-fixed panel and retrained models, most recently 2026-08-04 for the `s2_ndvi_anom` leakage fix. **Sign issue from the earlier (pre-2026-07-31) unconstrained model is fixed** via the monotonic constraint on export_banned/export_duty_pct/mep_usd_per_tonne (Script 15/23). Current state: median response collapses to ~0.0% for these scenarios (correct sign, but the constrained model can't recover a realistic magnitude either — see Script 31). |
| `31_Synthetic_DID_Policy_Effect.py` | Part A/B: `table_sdid_policy_effect.csv`, `table_sdid_unit_weights.csv`, `fig_sdid_treated_vs_synthetic.png`, `table_sdid_hub_policy_effect.csv`, `fig_sdid_hub_vs_nonhub.png`. Part C: `table_sdid_event_study.csv`, `fig_sdid_event_study.png`, `table_sdid_donor_robustness.csv`, `table_sdid_arrivals_effect.csv`, `fig_sdid_arrivals_treated_vs_synthetic.png` | 2026-08-01 | 🟢 | Estimates the 2023-24 onion export-restriction episode's price effect directly via Synthetic DID (Arkhangelsky et al. 2021), since Script 30's forecasting model can't recover a reliable magnitude (identification problem — duty/MEP/ban only ever moved together once). Part A (cross-crop, tomato/potato as donors): escalation-phase ATT +56.2% is credible (huge gap vs tomato placebo -30.7%), but the post-ban ATT (+14.5%) **fails its own placebo check** — indistinguishable from same-window tomato (+14.5%) and potato (+15.2%) placebo ATTs. Part B (within-onion, Nashik export-hub vs non-hub markets): real 2023 postban ATT +3.7% vs placebo-in-time (fake 2021 dates) ATT -3.9%. **Part C:** (1) event-study trajectories — decomposes each static ATT into a weekly path; (2) jackknife 90% CIs — the Part A postban placebo failure becomes a formal result: onion 90% CI [+12.2%, +16.8%] fully overlaps tomato-placebo's [+12.3%, +16.8%]; the escalation-phase CI [+52.3%, +60.1%] does NOT overlap tomato-placebo's [-31.8%, -29.6%]. Part B's hub-design CIs also separate: real-2023 postban [+0.9%, +6.6%] vs. placebo-in-time [-6.3%, -1.4%] do NOT overlap; (3) donor-pool robustness — escalation-phase ATT stable under leave-one-donor-out: [+46.1%, +60.8%] vs. full-sample +56.2%; (4) arrivals/quantity effect — onion arrivals in treated markets *fell* relative to synthetic control (escalation -25.8%, postban -14.3%), reported as an open finding, not resolved. **Overall honest conclusion: no single design here fully isolates the ban's causal magnitude.** Formally closed as a documented negative/non-identifiable result (2026-08-05). See Scripts 38/39 below for the two follow-up robustness checks this motivated. |
| `38_SDID_InSpace_Placebo_Test.py` | `table_sdid_inspace_placebo.csv`, `fig_sdid_inspace_placebo.png` | 2026-08-20 | 🟢 | Standard synthetic-control falsification test (Abadie, Diamond & Hainmueller 2010): every individual tomato/potato market treated as a placebo unit, one at a time, against the same donor pool. Real onion postban ATT judged against the resulting null distribution: p=0.131 — does not clear significance against this stronger test either, consistent with Part C's jackknife-CI finding above. **Patched 2026-08-20 for the market_id collision fix** (missed in the original nine-script pass) and re-run — result bit-for-bit identical to the pre-fix version (n=214 placebo markets, same mean/std/p-value). |
| `39_SDID_Stacked_MultiEpisode_EventStudy.py` | `table_sdid_stacked_event_study.csv`, `table_sdid_stacked_summary.csv` | 2026-08-20 | 🟢 | Extends the single-episode (2023-24) SDID design to all 3 verified onion export bans in the panel window (Sep 2019, Sep 2020, 2023-24), addressing the underlying n=1 identification problem by pooling 3 independent episodes into one event study. **Patched 2026-08-20 for the market_id collision fix** and re-run — result bit-for-bit identical to the pre-fix version. |
| `38b_SDID_Arrivals_InSpace_Placebo_Test.py` | `table_sdid_arrivals_inspace_placebo.csv`, `fig_sdid_arrivals_inspace_placebo.png` | 2026-08-21 | 🟢 | New. Arrivals-outcome analogue of Script 38, same in-space design applied to log1p(arrivals). Real ATT recomputation matched Script 31 Part C.4's postban row exactly (-0.1548 log-pts, -14.3%) before trusting the placebo result. 169 donor markets (fewer than 38's 214 — arrivals reporting is patchier than price). Placebo std 0.492 log-pts (~5x noisier than price's 0.096). Result: **fails decisively**, p=0.680 (115/169 placebo markets at least as extreme). |
| `39b_SDID_Arrivals_Stacked_MultiEpisode_EventStudy.py` | `table_sdid_arrivals_stacked_event_study.csv`, `table_sdid_arrivals_stacked_summary.csv`, `fig_sdid_arrivals_stacked_event_study.png` | 2026-08-21 | 🟢 | New. Arrivals-outcome analogue of Script 39, per-episode arrivals requalification (95% coverage, gaps <=4wk interpolated, mirroring Script 31 Part C.4). All 3 episodes produced usable fits (onion: 51/45/40 arrivals-qualifying markets for 2019/2020/2023). Onion's stacked mean sits outside the tomato/potato placebo band in 10/13 post-ban week-bins — but wrong-signed (arrivals fell, not rose, under an export ban meant to retain domestic supply) and already negative pre-ban. Read as corroborating the reverse-causality finding, not an independent effect — see 2026-08-21 note above. |
| `46_Directional_Accuracy_Test.py` | `table_directional_accuracy.csv`, `table_directional_accuracy_naive.csv`, `fig_directional_accuracy.png` | 2026-09-03 (re-run) | 🟢 | Re-run against the 2026-09-03 `dm_market_level_predictions.csv` refresh — clean, headline pattern unchanged (still all 24 cells significant vs. 50% null, potato M0 still pulls ahead of M6 at long horizons). Synced to `web/data/table_directional_accuracy.csv`. Originally: fills a real gap — every other metric in the project (RMSE/MAE/MAPE/R2/MASE) is magnitude-based; this is the first test of whether the model calls the right price DIRECTION. Per-market (`dm_market_level_predictions.csv`, M0/M6 only), origin price looked up from the raw panel, binomial-tested vs. 50% (all 24 cells significant, n=17k-153k each). Cross-validates the existing ablation story on an independent metric: potato's M0 pulls decisively ahead of M6 at long horizons (h=26w: 78% vs 65%), matching the "richer features don't help potato" finding; tomato/onion directional accuracy generally holds or improves with horizon (tomato M6 84% at h=13w). B1_Naive scores exactly 0.0% everywhere by construction (always predicts "no change") — crop-level context baseline, not a fair per-market comparison. |
| `47_Market_Level_Accuracy.py` | `table_market_level_accuracy.csv` | 2026-09-03 (re-run) | 🟢 | Re-run against the 2026-09-03 `dm_market_level_predictions.csv` refresh (1,039,953 rows, 0 unmatched to a state) — 6,844 market cells + 194 state cells (was 6,788/194), 511 thin markets (7.5%) still meaningfully shrunk. Synced to `web/data/table_market_level_accuracy.csv` and verified live (dashboard's "This market"/"State" figures moved by ~1-2 points, e.g. potato Uttarakhand market ~75%→~74%). Hierarchical crop -> state -> market accuracy from real backtest predictions (`dm_market_level_predictions.csv`, M6 only) joined to `production_models/reference_rows.csv` for the market->state map, with two-level empirical-Bayes shrinkage (market toward its own state, state toward crop-wide) so every cell gets a trustworthy figure instead of a hidden or noisy one. **Switched from MAPE to WAPE (user-requested, after Script 50's empirical comparison)**: plain MAPE (mean of per-row percentage errors) let a single near-zero-price row dominate a market's whole figure — e.g. Patti APMC (tomato, h=26w) showed MAPE=224% against a WAPE of 38% for the identical predictions, traced to a handful of rows with an anomalous recorded price (Rs 15/quintal) against an otherwise-reasonable ~Rs 2,289 model prediction. WAPE (`sum(abs_err)/sum(y_true)`) uses the same errors weighted by each row's actual value instead of counted equally. Columns renamed `*_mape_*` -> `*_wape_*` throughout (not just relabeled -- the underlying formula changed from mean-of-ratios to ratio-of-sums). Feeds the React dashboard's "This market: ~X% · State: ~Y%" figures alongside the crop+horizon-wide "Model Accuracy" KPI (also WAPE-based, see Script 23). 6,788 market cells + 194 state cells. Not byte-identical to the actual deployed production models (Script 23 trains separately) — same feature config/CV scheme, the best available real per-market proxy since no production model has its own stored per-market backtest. |
| `50_MAPE_vs_WAPE_Comparison.py` | `table_mape_vs_wape_cropwide.csv`, `table_mape_vs_wape_market.csv`, `fig_mape_vs_wape_market_scatter.png` | 2026-09-03 (re-run) | 🟢 | Re-run against the 2026-09-03 `dm_market_level_predictions.csv` refresh — comparison-only, confirms no production metric changed (348/6,754 market cells, 5.2%, still differ by >=10 points between MAPE/WAPE; Patti APMC still the worst case). Originally: new. User asked why "Model Accuracy" used MAPE rather than WAPE. Computed both metrics from the SAME already-existing M6 backtest predictions (`dm_market_level_predictions.csv`, no retraining) to check empirically before switching anything. Crop-wide: WAPE consistently lower than MAPE across all 12 crop x horizon cells (mean gap 2.6pts, max 5.65pts at tomato h=26w: 44.8% MAPE vs 39.2% WAPE) — not just a thin-market artifact, shows up in the pooled hundred-thousand-row figures too. Market-level: high correlation overall (0.92 across 6,742 cells with n>=10), but 343 cells (5.1%) differ by >=10 accuracy points, worst case Patti APMC (tomato, h=26w) at a 185-point gap (MAPE 224% vs WAPE 38%), traced to a handful of near-zero recorded prices. Result: switched to WAPE everywhere (Scripts 23/47, dashboard) same day -- see those rows. |
| `48_State_vs_Shared_Model_Comparison.py` | `table_state_vs_shared_model_dm_test.csv`, `state_model_predictions.csv` | 2026-08-31 | 🟢 | New. Empirical test of "should we fit one model per (crop, state) instead of one shared pooled model" — trains a state-restricted M6 model for the top-2 highest-data states per crop (best-case for the per-state argument), same 5-fold rolling-origin CV/hyperparameters as the shared model's own recorded backtest, DM-tests it against the shared model's predictions for identical (market, week) cells. Result: shared model wins 15/24 comparisons significantly (p<0.05) vs. 5/24 for state-restricted, confirming the bias-variance argument for pooling — cutting training data to one state costs more from variance (thinner data, ~60-feature M6 set) than it gains from removing cross-state dilution, even in the best-data states. |
| `49_Market_Level_Model_Comparison.py` | `table_market_restricted_vs_shared_model_dm_test.csv`, `market_restricted_model_predictions.csv` | 2026-09-01 | 🟢 | New. Direct follow-up to Script 48, pushed one level further — does a model restricted to ONE market's own data beat the shared model? Tested on the 3 fullest-history markets per crop (504/504 weeks, no gaps — all 9 are also Script 42's own "market leader" markets), including both Uttarakhand potato markets to specifically test whether Script 48's one state-level win holds market-by-market. Same 5-fold rolling-origin CV/hyperparameters as the shared model's backtest, DM-tested on identical cells. Result: shared model wins 9/36 significantly, market-restricted wins **0/36**. Uttarakhand's state-level win does NOT replicate per-market — Kashipur APMC shows the same directional pattern but never reaches significance (too little data for DM test power at ~170 weeks), while Dehradoon APMC is significantly worse restricted at short horizons. Settles the per-market question: full pooling beats partial pooling (state-level, occasionally) beats no pooling (per-market, never observed to win). |
| `55_DM_Test_M6_vs_M7.py` | `table_dm_m6_vs_m7_detail.csv`, `table_dm_m6_vs_m7_summary.csv` | 2026-09-13 | 🟢 | New. Formal market-level DM significance test of the M7 (drought) layer, mirroring Script 18b's exact methodology (same DM function, same per-market granularity, same Fisher-combined-p) with `DIAGNOSTIC_PAIR=('M6','M7')` instead of `('M0','M6')`. 6,554 market-level tests across 1,024 markets. **Substantially revises the raw-metric read from Script 15's row above — this is the more trustworthy result.** Two headline findings: (1) **Potato h=13w's raw +0.30 R2 gain, the ablation table's best-looking M7 result, does NOT survive per-market testing** — only 2.4% of markets significant, split 1 M7 / 1 M6, Fisher p=0.46. That crop-level number was likely driven by a small number of high-volume/West-Bengal markets or an aggregation artifact, not a broad effect. (2) **Onion h=1w and potato h=1w — cells the raw table showed as nearly flat (+0.003/+0.006 R2) — have real, large, statistically robust improvements the crop-level average was hiding**: onion h=1w is 31.1% significant with M7 favored 205-to-35 (Fisher p≈0), and potato h=1w is 75.6% significant with M7 favored in every single significant market, 62-to-0 (Fisher p≈0). Elsewhere mixed and mostly small: tomato h=1w/h=13w lean M7 (13.3%/6.0% significant, M7-favored), tomato h=4w/h=26w and onion h=26w lean M6 (M6-favored in the majority of their significant markets), onion h=4w and potato h=4w/13w/26w sit at or near the ~5% false-positive floor with no reliable direction. `dm_market_level_predictions.csv`/`dm_market_level_raw_results.csv` were temporarily regenerated with `DIAGNOSTIC_PAIR=('M6','M7')` for this run, then restored to the resting `('M0','M6')` state from a pre-run backup (not a fresh regenerate) — verified byte-identical to the pre-existing committed `dm_market_level_raw_results.csv` afterward, so Scripts 18b/46/47/50 are unaffected. **Superseded as the basis for any production decision by the 2026-09-14 fold-level follow-up below — its per-market significance counts turned out to be inflated by a structural artifact (see next row).** |
| *(ad hoc analysis, no saved script — see below)* | none saved; computed directly against a temporarily-regenerated `dm_market_level_predictions.csv` (`DIAGNOSTIC_PAIR=('M6','M7')`, restored to resting `('M0','M6')` afterward, same backup/restore discipline as Script 55's row above) | 2026-09-14 | 🔴 | **M7 production-wiring decision: investigated, NOT wired in — insufficient independent evidence, not a negative finding about drought data itself.** Following up on Script 55's per-market significance table (row above) with a fold-level robustness check surfaced a structural problem with that table: for every crop x horizon cell, **fold 1's M6 and M7 predictions are byte-identical** (0.000% difference) — IDM CDI coverage only starts 2021-07-14 and VEDAS Trigger-1 only starts Kharif 2022, both AFTER fold 1's 2021-06-30 training cutoff, so fold 1's "M7" model trained on zero real drought signal and is mathematically the same model as M6. That leaves only 4 of the 5 CV folds (2-5) carrying any real information about M7's effect — meaning Script 55's ~750-800 "per-market" significance counts were never ~750-800 independent trials; every market within a fold is scored by the SAME single fitted model, so hundreds of markets moving together in one direction reflects at most 4 real underlying (fold-level) events, not hundreds of independent confirmations. Re-checked properly: for every one of the 12 crop x horizon cells, counted how many of the 4 real folds have M7 with lower pooled MSE than M6. Best result across all 12 cells: **3 of 4 folds** (onion h=4w, tomato h=13w, potato h=13w) — a binomial test on n=4 gives p=0.625 even for that best case, nowhere near significance. Potato h=1w, the single strongest-looking result in Script 55's table (75.6% of markets significant, 62-0 unanimous), wins in only **1 of the 4 real folds** at the fold level — its apparent per-market "unanimous" effect is exactly what you'd expect from one fold's model producing a small but consistent shift that gets replicated across ~4,000 (market, week) rows scored by that one model, not genuine market-by-market evidence. **No crop x horizon cell clears a defensible bar once the real independent sample size (4 folds, not 750+ markets) is used.** This is not evidence that drought data is uninformative — M7's data history (VEDAS from 2022, IDM from mid-2021) simply hasn't accumulated enough independent CV folds yet for this project's rolling-origin design to distinguish a real effect from fold-specific noise in either direction. **Decision: M7 stays out of `23_Train_Production_Models.py` for now.** Revisit once VEDAS/IDM have enough additional years for more folds, or if the CV design is changed (e.g. nested/blocked CV within each fold) to extract more independent trials from the same years. `dm_market_level_predictions.csv`/`dm_market_level_raw_results.csv` restored to resting `('M0','M6')` state afterward, verified via `git status` showing no unintended change; Script 15's `MARKET_LEVEL_DIAGNOSTIC`/`DIAGNOSTIC_PAIR` reverted to `False`/`('M0','M6')`. |
| `15_Ablation_Study_M0_M4.py` (re-run, M8 added) | `ablation_raw_results.csv`, `table_ablation.csv`, figures | 2026-09-21 | 🟢 | Extended M0→M7 to add M8 = M6 + Fertilizer MRP (Urea/DAP/MOP, Scripts 57/59), branching from M6 rather than the already-excluded M7 (see script docstring: stacking on a rejected layer would entangle two independent questions). CPI-AL/RL (acquired alongside fertilizer) deliberately excluded from M8 — checked directly before running anything: it only exists from Jun-2025, so its training window covers 0 of 5 CV folds' cutoffs except a 1-month sliver in fold 5, the same problem M7's fold 1 had but affecting nearly every fold instead of one. Deferred until it has 2-3 years of history, same standard M7 was held to (user confirmed this scoping 2026-09-21 before the run). Fertilizer MRP uses the same missingness-flag treatment as M7's drought features (not forward-fill) since its ~1-year pre-2018 gap at the panel's front is core coverage, not a recent-data lag. 540 LightGBM fits (9 variants x 5 folds x 4 horizons x 3 crops), 88.6 min, clean run. Raw crop-level read: mixed, small, sign-flipping across horizons per crop (e.g. tomato h=26w R² -0.107→0.136, but h=13w 0.135→0.077) — the same shape of raw result that turned out to be misleading for M7, so not trusted without the market-level + fold-level follow-up below. |
| `60_DM_Test_M6_vs_M8.py` | `table_dm_m6_vs_m8_detail.csv`, `table_dm_m6_vs_m8_summary.csv` | 2026-09-21 | 🟢 | New. Market-level DM significance test of M8 (fertilizer MRP), mirroring Script 55's M6-vs-M7 methodology exactly, with `DIAGNOSTIC_PAIR=('M6','M8')`. 6,554 market-level tests across 1,024 markets. Fold-level identity check (added to this script, since M7's version of this check was what actually caught its fold-1 problem) confirms M8 trained on genuine fertilizer signal throughout — only ~0.01% byte-identical predictions in every one of the 5 folds, unlike M7's fold-1 100%. Raw per-market read looked dramatic in places: tomato h=1w 35.2% of markets significant (256 M8-favored vs 27 M6), h=26w 11.6% significant (87 M8 vs 3 M6) — on the surface, M7-caliber evidence. **Superseded by the same day's fold-level follow-up below — these per-market counts turned out to be inflated by the identical structural artifact that fooled Script 55's read of M7.** |
| *(ad hoc analysis, no saved script — see below)* | none saved; computed directly against `dm_market_level_predictions.csv` (`DIAGNOSTIC_PAIR=('M6','M8')`, restored to resting `('M0','M6')` afterward via a pre-run backup, same discipline as the M7 precedent) | 2026-09-21 | 🔴 | **M8 production-wiring decision: investigated, NOT wired in — insufficient independent evidence, mirrors M7's exact verdict and for the same structural reason.** Following up on Script 60's per-market table with the same fold-level pooled-MSE check that overturned Script 55's M7 read: for every one of the 12 crop x horizon cells, counted how many of the 5 real folds have M8 with lower pooled MSE than M6. **No cell wins in more than 3 of 5 folds** (binomial p=1.000 for every 3/5 result — nowhere near significance at any reasonable threshold). This includes tomato h=1w and h=26w, the two cells whose per-market tables looked the most one-sided (256-27 and 87-3) — both win in only 3/5 folds at the fold level, confirming those lopsided market counts are exactly the "one fold's shift replicated across hundreds of market-weeks" artifact M7's potato h=1w turned out to be, not genuine broad evidence. Unlike M7, this isn't a data-recency problem (fertilizer MRP has real training coverage in all 5 folds, confirmed by Script 60's fold-level identity check) — the effect size itself is just too small and inconsistent in sign to distinguish from fold-to-fold noise with only 5 independent trials. **Decision: M8 stays out of `23_Train_Production_Models.py` for now.** Revisit if/when the CV design changes to extract more independent trials from the same years (e.g. nested/blocked CV within each fold), same open question left for M7. CPI-AL/RL was never tested (see Script 15's row above) and remains a separate, still-open question once it has enough history. |
| *(ad hoc analysis, no saved script — see below)* | none saved; computed directly against the resting `dm_market_level_predictions.csv` (`DIAGNOSTIC_PAIR=('M0','M6')`, its resting/committed state, no regeneration needed) | 2026-09-21 | 🔴 | **Foundational check, requested after a user question about the whole project's premise: does the CURRENTLY-DEPLOYED stack (M6 = M2 macro + M3 climate + M4 satellite + M5 infra + M6 policy) actually beat price-only (M0) at the fold level? Never checked before — Script 18b's per-market table was the only recorded evidence, and per M7/M8's findings, per-market tables are exactly the kind of result that can't be trusted without this check.** Ran the identical fold-level pooled-MSE comparison used for M7/M8, this time M0 vs M6. **Result: no crop x horizon cell reaches significance in favor of M6.** Best cases (tomato h=1w/h=26w, potato h=4w) win only 4 of 5 folds (p=0.375, not significant). Most cells are worse: 2-3 of 5 folds, p=1.000. **Potato h=26w is the most striking: M0 wins all 5 of 5 folds (0/5 for M6), p=0.062** — trending toward M0 being significantly BETTER than the full production model, not just tied. This means the entire currently-deployed layer stack has never been shown, by this project's own now-established standard of evidence, to beat price-only forecasting. Not treated as a final verdict on the layers' worth (5 folds is a small sample — this design can only detect large effects, so this is as much a power problem as a null result) but recorded here because it changes what "success" means for this line of work and directly motivated the Track A diagnostic below. |
| `61_M7_M8_Feature_Diagnostic.py` | `table_shap_m7_m8_layer_features.csv`, `table_fert_drought_redundancy.csv` | 2026-09-21 | 🟢 | New. Track A of the strategic review above: before concluding "these layers carry no signal" or committing to an expensive CV redesign for more statistical power, checked two cheaper questions on data already in hand. Trained one full-history model per (crop, horizon) for M7 and M8 (same train/early-stop/refit recipe as Script 23; neither M7 nor M8 has a saved production artifact) and ran SHAP + LightGBM split-count analysis, plus a Pearson-correlation redundancy check of every M7/M8 feature against existing macro/WPI/trend features. **Two different stories for the two layers.** (1) **Drought (M7) is a genuine non-usage problem**: trigger1/cdi and their missingness flags rank in the bottom quarter of ~82 features in every cell (median rank 66-75/82), with very low split counts (2-4, except cdi at 116) — the model mostly isn't using this signal at all, consistent with the sparse-coverage story already established (Scripts 51-54). (2) **Fertilizer (M8) is a genuine usage-but-redundancy problem, which is a materially different and more actionable finding**: `fert_mop_mrp` (Muriate of Potash) gets real split volume (143 splits, median rank 23/84 — top third of all features, ranked 6th of 84 outright for tomato h=26w) and `fert_dap_mrp` similarly mid-table (94 splits, rank 40/84) — these ARE being used. But both correlate strongly with `year_trend` (r=0.88-0.89) and other slow-moving macro series (agri wages, diesel, crude oil, all r=0.6-0.84) — MOP's price genuinely has trended upward over 2018-2026 in a way that looks a lot like "a noisy proxy for calendar time" to a tree model that already has an explicit `year_trend` feature. Consistent with this, `fert_mop_mrp`'s SHAP sign flips across horizons for the same crop (e.g. tomato: negative at h=1/13/26w, positive at h=4w) — a genuine cost-push effect should push price the same direction regardless of horizon, so sign instability further supports "used as a rough trend proxy, not a stable causal driver" rather than "no real relationship." (`fert_urea_mrp` stays low-usage throughout, expected: it's administratively fixed at Rs 5,360/MT for nearly the whole sample, so it carries almost no split-worthy variance — not a modeling failure, a real feature-of-the-data property. The three `*_missing` flags are essentially dead weight, 0-1 splits each, since real coverage starts 2018 and the panel's own pre-2018 rows are a small fraction of total history.) `cdi_missing`'s strong correlation with several macro columns (r=-0.6 to -0.82) is a trend artifact, not an economically meaningful redundancy — it's a step function at IDM's 2021-07-14 start date, and any monotonically-drifting macro series will correlate with a step function over a long enough window; not read as evidence about drought itself. **Suggested next experiment, not yet run**: re-test fertilizer MOP/DAP as a detrended/differenced feature (deviation from its own trend, or period-over-period % change) rather than raw level, to isolate genuine short-term cost shocks from the generic drift `year_trend` already captures, then re-run the Script 60 fold-level check on that engineered feature before deciding whether the redundancy is fixable or fundamental. |
| `56_NOAA_ONI_Layer.py` (context) + `62_ONI_Panel_Prep.py` + `63_ONI_Feature_Diagnostic.py` | `data/noaa_oni/oni_panel_monthly.csv`, `table_shap_m9_oni_features.csv`, `table_oni_redundancy.csv` | 2026-09-21 | 🟢 | New. Part 1 of the combined Track A follow-up (user asked "what about NOAA ONI and other new layers" after the M7/M8 findings). ONI had never been joined onto any panel before this — Script 62 reshapes Script 56's overlapping 3-month-season acquisition into a plain (year, month) series plus `oni_lag_3m`/`oni_lag_4m` (the lagged versions the original review docs specifically recommended, since ENSO's effect on supply shows up months after the anomaly, not the same month) — validated against the same known-event checks Script 56 used (Feb-1998 +2.22, Feb-2016 +2.50, both reproduced exactly after the season->month remap). `panel_layers.py` gained `join_oni()`, same shape as `join_macro()` (national, (year,month) key, no missingness-flag treatment needed since ONI has continuous coverage back to 1950). Ran the same Script-61-style usage+redundancy diagnostic before any expensive ablation run — **result is a genuinely different, more promising profile than either M7 or M8**: all three ONI features rank in the top quarter of ~81 features (median rank 18-22/81, split counts 196-276 — far higher usage than M7's drought or M8's fertilizer got), AND zero meaningful redundancy with any existing macro/WPI/trend feature (no correlation >=0.3 found at all, vs. fertilizer's 0.6-0.89). SHAP sign is "mixed" across cells here too, but unlike fertilizer's cost-push mechanism (which should push price the same direction regardless of horizon), ENSO's known agronomic effect genuinely is crop/season-dependent in India (El Niño's drought association mainly affects Kharif-season conditions, differently across crops/regions), so sign variation by crop/horizon is an expected feature of a real climate teleconnection, not read as a redundancy red flag the way it was for MOP. **Recommendation: ONI is the strongest candidate of the three new layers investigated so far and warrants the full ablation + DM significance test (the treatment M7/M8 got), not further diagnosis** — real usage, no confound, a plausible causal mechanism (ENSO -> monsoon/growing conditions -> supply) with directional variation that matches known agronomy rather than looking like a proxy artifact. Not yet run as of this entry. |
| `15_Ablation_Study_M0_M4.py` (re-run, M9 added) | `ablation_raw_results.csv`, `table_ablation.csv`, figures | 2026-09-21 | 🟢 | Part 2 of the combined Track A follow-up: full ablation extended to add M9 = M6 + NOAA ONI, branching from M6 (sibling of M8, same rationale as M8's own docstring note — stacking on the excluded M7 would entangle two independent questions). 600 LightGBM fits (10 variants x 5 folds x 4 horizons x 3 crops), 40.4 min, clean run. Raw crop-level read is the most encouraging of the three new layers so far: onion and potato both show consistent RMSE/R² improvement over M6 at longer horizons (onion h=26w: RMSE 1115.9->1005.3, R² 0.098->0.249; onion h=13w: R² 0.369->0.377; potato h=1w: RMSE 199.6->192.1, R² 0.747->0.756; potato h=13w: RMSE 651.2->593.3, R² still negative but improved -2.193->-1.627), tomato roughly flat to slightly worse (h=1w R² 0.623->0.628, h=4w 0.301->0.271). Same caveat as M7/M8's raw tables applied on principle — not trusted without the market-level + fold-level follow-up below, even though the pattern here is visually more consistent than M7/M8's sign-flipping. |
| `64_DM_Test_M6_vs_M9.py` | `table_dm_m6_vs_m9_detail.csv`, `table_dm_m6_vs_m9_summary.csv` | 2026-09-21 | 🟢 | New. Market-level DM significance test of M9 (NOAA ONI), mirroring Scripts 55/60's methodology exactly, with `DIAGNOSTIC_PAIR=('M6','M9')`. 6,554 market-level tests across 1,024 markets. Fold-level identity check confirms M9 trained on genuine ONI signal throughout — only ~0.01-0.02% byte-identical predictions in every one of the 5 folds (expected, since ONI has continuous coverage back to 1950 with no fold-1-predates-the-data problem). Raw per-market read looked strong in several cells: onion h=1w 34.5% of markets significant (254 M9-favored vs 12 M6, Fisher p≈0), potato h=1w 61.0% significant (50-0 unanimous, Fisher p≈0), tomato h=26w 10.0% significant (73-5). **Superseded by the same-day fold-level follow-up below — per Scripts 55/60's established lesson, these per-market counts cannot be trusted at face value.** |
| *(ad hoc analysis, no saved script — see below)* | `table_foldlevel_m6_vs_m9.csv`; computed directly against `dm_market_level_predictions.csv` (`DIAGNOSTIC_PAIR=('M6','M9')`, restored to resting `('M0','M6')` afterward via a pre-run backup, same discipline as the M7/M8 precedent) | 2026-09-21 | 🔴 | **M9 production-wiring decision: investigated, NOT wired in — one cell clears the bar, the rest don't.** Following up on Script 64's per-market table with the same fold-level pooled-MSE check that overturned Scripts 55/60's M7/M8 reads: for every one of the 12 crop x horizon cells, counted how many of the 5 real folds have M9 with lower pooled MSE than M6. **Only onion h=1w reaches significance: 5 of 5 folds (binomial p=0.031)** — a real, broad, fold-consistent effect, not a single-fold artifact replicated across markets. Every other cell falls short: best runners-up are 4/5 (tomato h=1w, tomato h=26w, onion h=26w, all p=0.188, not significant at any reasonable threshold), most sit at 2-3/5 (p=0.5-0.812). Potato h=1w — the most one-sided per-market result in Script 64's table (50-0 unanimous, 61.0% significant) — wins in only 3 of 5 folds at the fold level (p=0.5), the same "one fold's shift replicated across hundreds of market-weeks" artifact that fooled M7's potato h=1w and M8's tomato cells. **Decision: M9 stays out of `23_Train_Production_Models.py` for now** — one significant cell out of twelve, with no ability at n=5 to distinguish "real but narrow ENSO effect on onion's shortest-horizon forecast" from a single lucky fold, isn't enough to justify adding a full feature layer to the production stack on its own. Revisit alongside M7/M8 if/when the CV design changes to extract more independent trials from the same years — worth noting onion h=1w's result is the single strongest fold-level significance found across all three new layers (M7, M8, M9) investigated this round, so it's the best candidate to prioritize if that CV redesign happens. `dm_market_level_predictions.csv`/`dm_market_level_raw_results.csv` restored to resting `('M0','M6')` state afterward, verified via `git status` showing no diff; Script 15's `MARKET_LEVEL_DIAGNOSTIC`/`DIAGNOSTIC_PAIR` reverted to `False`/`('M0','M6')`. |
| *(ad hoc analysis, no saved script — see below)* | `table_foldlevel_m6_vs_m7_v2.csv`; computed against a temporarily-widened fold 1 (`train_end` nudged 2021-06-30 -> 2021-10-31, `val_start` 2021-07-01 -> 2021-11-01), `DIAGNOSTIC_PAIR=('M6','M7')`, restored to resting state afterward via a pre-run backup | 2026-09-22 | 🔴 | **CV-redesign follow-up, part 1: does fixing M7's degenerate fold 1 change its verdict? No — the effect is just too small, not a fold-1 artifact.** Fold 1's original `train_end` (2021-06-30) predated IDM CDI's real start (2021-07-14) by 2 weeks, making it byte-identical for M6 vs M7 (0.00% distinct predictions, confirmed 2026-09-14). Nudged `train_end` to 2021-10-31 -- recovers ~15 weeks of real IDM signal while keeping a 2-month validation window (Nov-Dec 2021) rather than eliminating it; confirmed the fix worked (fold 1 now 0.01% byte-identical, same level as the other real folds). This can never recover VEDAS Trigger-1 (Kharif-2022+ only, which postdates fold 1's own 2022 test window -- no boundary nudge can include it without leaking test-year data into training). Re-ran the fold-level pooled-MSE check with all 5 folds now real: **no crop x horizon cell reaches significance.** Best cases improved somewhat from the original 4-fold read (tomato h=1w/13w/26w now 4/5, p=0.188, vs. the original best of 3/4, p=0.625) but nowhere near significant. **Verdict unchanged: M7 stays out of production.** The improvement in P-value came from having one more real fold to test, not from fold 1 itself revealing new evidence -- M7's problem was already correctly diagnosed as "genuinely too small an effect to distinguish from noise at n=5," not "an artifact of the degenerate fold." `dm_market_level_predictions.csv`/`dm_market_level_raw_results.csv` and Script 15's `FOLDS`/`MARKET_LEVEL_DIAGNOSTIC`/`DIAGNOSTIC_PAIR` restored to resting state afterward, verified via `git status` showing no diff. |
| *(ad hoc analysis, no saved script — see below)* | `table_foldlevel_m6_vs_m9_cvext.csv`; computed against a temporarily-extended 8-fold `FOLDS` list (3 new folds added before the canonical fold 1, testing 2019/2020/2021 using panel history that otherwise sits unused as pure training warmup), `DIAGNOSTIC_PAIR=('M6','M9')`, restored to resting state afterward via a pre-run backup | 2026-09-22 | 🟢 | **CV-redesign follow-up, part 2: extending M9's fold count from 5 to 8 CONFIRMS onion h=1w's significance rather than diluting it — the strongest robustness check this finding has had.** The panel runs 2017-01 through 2026-08/09, but the canonical fold 1 doesn't start testing until 2022 -- 2017 through mid-2021 was pure unused training warmup. Since NOAA ONI has zero coverage gap (continuous since 1950), 3 new folds were added testing 2019/2020/2021 (`train_end` 2018-06-30/2019-06-30/2020-06-30) -- a 4th candidate (test year 2018) was deliberately excluded, its training window would only be 6 months, too thin to trust. This redesign is NOT valid for M7/M8, whose real coverage starts within or after this window (see the layer-by-layer breakdown this same investigation surfaced). Result: **onion h=1w now wins 7 of 8 folds (binomial p=0.035)**, holding significance despite 3 additional, independently-drawn folds from a different, earlier part of the panel's history -- if the original 5/5 result had been a fluke of that specific fold set, adding 3 more folds from 2019-2021 would have been expected to dilute it below significance, and it didn't. No other crop x horizon cell newly crosses significance (onion h=26w improved to 6/8, p=0.145, still short). **This doesn't change the production-wiring decision on its own** (still one cell out of twelve) but meaningfully raises confidence that onion h=1w's ENSO effect is real rather than a 5-fold coincidence, reinforcing it as the priority candidate if/when M9 (or a similar ONI-only feature) is revisited. Sanity-checked: folds 5-8 in the new numbering (= the original folds 1-4) reproduced byte-identical M6/M9 metrics to the original run, confirming the fold renumbering didn't corrupt anything. `dm_market_level_predictions.csv`/`dm_market_level_raw_results.csv` and Script 15's `FOLDS`/`MARKET_LEVEL_DIAGNOSTIC`/`DIAGNOSTIC_PAIR` restored to resting state afterward, verified via `git status` showing no diff. |
| `.github/workflows/weekly_refresh.yml`, `scripts/14_Satellite_Climate_Features.py` | n/a (infra fix) | 2026-09-24 | 🟢 | **Security incident (contained) + root-cause fix, first real GEE-automated CI run (35713799320).** The run's own PR (#5) was found to contain a leaked GEE service-account private key (`gee-service-account-key.json`) -- `peter-evans/create-pull-request`'s "sweep up uncommitted changes" fallback pulled it in (same root-cause class as the 2026-09-16 54MB-CSV incident, different file). **Google's own secret-scanning auto-detected and disabled the key within the same session it was created** -- exposure window real but short, contained before this was even noticed. Response: old key deleted, new key generated, `GEE_SERVICE_ACCOUNT_KEY` GitHub secret rotated, PR #5 closed unmerged (still contains the leaked key in its diff/history), its branch deleted. **Root cause fixed**: the key file is now written to `$RUNNER_TEMP` (outside `$GITHUB_WORKSPACE` entirely) instead of the repo root -- the only fix that's reliable regardless of `.gitignore` or any tool's own fallback behavior. Added a defense-in-depth `.gitignore` pattern too. **Separately, the same run revealed Script 14 crashes in CI**: it needs one-time, local-only historical bulk pulls (2000-2024 ERA5/CHIRPS/S2/MODIS zips, ~33MB, plus potato zone relocation history, ~6MB) that were never in Drive or git (`data/satellite_climate/raw/` is deliberately `.gitignore`'d). Fixed by uploading both to a new Drive folder (`TOP_Digital_Twin_Satellite_Historical`) and adding a sync step to the workflow, mirroring the existing raw market-data pull; `extract_zip()` in Script 14 now falls back to already-extracted files in the destination instead of crashing when the zip itself isn't present (which it never will be in CI). Validated the fallback logic locally (missing-zip-with-existing-files returns correctly; missing-zip-with-nothing-to-fall-back-on still raises clearly) before trusting it. **`RCLONE_CONFIG` GitHub secret also needs re-uploading** with a freshly-reconnected token (the local rclone Drive OAuth token had independently expired the same day, needing a human browser reconnect) -- not yet done, needs explicit user action to write to the secret store. Not yet re-triggered/verified end-to-end after this fix; next run should confirm both fixes actually work in CI. |
| *(none found)* | `table4_model_metrics.csv`, `fig_feature_importance.png`, `fig_actual_vs_pred_2024.png`, `test_predictions_2024.csv`, `lgbm_{tomato,onion,potato}.txt`, `market_list_by_crop.xlsx`, `fig_shap_{tomato,onion,potato}.png` (July 8 versions, not the July 29 SHAP figures) | 2026-07-08 | ⚪ | No script in the current `scripts/` folder produces these — orphaned from an early prototype, likely predating this repo's current script numbering. Safe to delete once confirmed unneeded; not referenced by README §3. |

## Exploratory, not part of the main numbered pipeline

### Two-phase residual architecture (Scripts 32-37) — REJECTED 2026-08-04
| Script | Outputs | Last generated | Notes |
|---|---|---|---|
| `32_LongHistory_Panel_Builder.py` | `data/agmarknet_weekly/longhistory/*.csv` | 2026-08-01 | One-off validation experiment: builds a 2003-2026 (23-year) version of the panel to test whether extending training history beyond the 2017 floor helps. Reused by Scripts 40/41 (escalation-signature prototype, see below) as a convenient long-history price source — that reuse is unrelated to the two-phase verdict below. |
| `33_LongHistory_Validation_Experiment.py` | `table_longhistory_validation.csv`, `table_longhistory_validation_raw.csv` | 2026-08-01 | Controlled comparison (same market set, same folds, same recipe) of a 9-year vs 23-year training window. Finding: real, modest improvement (9/12 crop x horizon cells, ~1.2pp average MAPE reduction) concentrated in onion (all 4 horizons) and tomato (except h=26w, where longer history measurably hurts — plausibly a 2003-2016 regime-break effect: pre-GST, pre-eNAM, pre-2016-demonetization). This motivated building out Scripts 34-37 to test the idea properly, rather than adopting it on this result alone — see the formal verdict below. |
| `34_Baseline_Phase_Panel_Join.py` | `data/baseline_phase_panel.csv` | 2026-08-04 | Full 2003-2026 joined panel for the two-phase Phase 1 model (all layers except Sentinel-2). |
| `35_TwoPhase_Baseline_Model.py` | `Model_Output/experiments/two_phase/table_baseline_phase_metrics.csv` (the underlying per-row OOF predictions file is gitignored — regenerate via script, too large for GitHub) | 2026-08-03 | Phase 1 (baseline) model, 9 annual expanding folds. Its raw MAPE beat M6 on several cells (notably onion) — see Script 37's formal test of whether that's real. **Not yet re-verified for the market_id collision bug** (groups by market name; see 2026-08-14 note above) — low priority since the architecture is already rejected regardless, but genuinely open. |
| `36_TwoPhase_Residual_Model.py` | `Model_Output/experiments/two_phase/table_twophase_combined_metrics.csv` (the underlying per-row predictions file is gitignored) | 2026-08-04 | **REJECTED.** Phase 2 (Sentinel-2-only residual on top of Phase 1) — combined forecast is worse than Phase 1 alone AND worse than M6 at nearly every crop/horizon cell, in both its full-feature and narrowed forms. Kept in the repo as a documented negative result, not part of the active pipeline. Same unverified collision-bug caveat as Script 35. |
| `37_DM_Test_M6_vs_Phase1Alone.py` | `table_dm_m6_vs_phase1alone.csv` | 2026-08-04 | Formal DM test of Phase-1-alone's apparent raw-MAPE edge over M6. **Result: does not survive formal testing (1/12 cells significant at p<0.05).** This is the decision record — M6 confirmed as the correct production choice, not merely "not yet adopted." |

### Early-warning prototype & market network (Scripts 40-42) — folded into the comprehensive review report as Sections 14-15, 2026-08-08/10
| Script | Outputs | Last generated | Notes |
|---|---|---|---|
| `40_Escalation_Signature_Head.py` | `table_escalation_signature_loeo_percrop.csv`, `table_escalation_signature_scores_percrop.csv`, `table_escalation_signature_placebo_test.csv`, `fig_escalation_signature_percrop_heldout.png`, `fig_escalation_signature_placebo_test.png` | 2026-09-02 (three-times-revised) | **Three independent audit-finding fixes, same day, across two review passes.** (1) LOEO train/test overlap: padding negatives near each event satisfied the same condition as `train_mask`, so they were trained AND tested on — fixed by excluding them from training. (2) Placebo-in-time scoring redesign (Section 4.5): the old test reused each episode's LOEO model to score the ENTIRE crop history — nearly every placebo candidate was scored in-sample — replaced with K=8-fold time-block CV giving every week a genuinely out-of-fold score. (3) **Label-leakage feature removed**: `price_vs_seasonal_norm` — the EXACT quantity thresholded to define the escalation label itself — was also being fed to the classifier as a raw input, a near-deterministic shortcut. Removed from `FEATURES` (kept in the dataframe for label construction only); also added the standard +1/+1 permutation-test floor adjustment to the placebo p-value (matches Scripts 38/38b's convention — a permutation p-value should never be exactly 0, since the real observation is always at least as extreme as itself). **Final honest result, re-run after all three fixes: mean LOEO AUC tomato 0.860 / onion 0.829 / potato 0.773 (per-episode discrimination remains genuinely good) — but ZERO of 8 episodes clear p<0.05 on the placebo-in-time test** (closest: onion_2019 p=0.208, potato_2014 p=0.216). This supersedes BOTH the original "5 of 8 significant" claim AND this session's own intermediate "only potato_2014 significant (p=0.000)" figure from before the label-leakage fix — update any deck/disclosure document/progress-review slide citing either. The escalation-signature detector discriminates real episodes from background weeks well in isolation; it does not, under this fully corrected methodology, show its real pre-intervention windows standing out significantly against the full permutation null at this data volume — an honest negative result on the significance test specifically, not a failure of the underlying signal. |
| `41_Escalation_Signature_Nashik_Hub_Test.py` | `table_escalation_signature_nashik_loeo.csv`, `table_escalation_signature_nashik_placebo.csv`, `fig_escalation_signature_nashik_vs_national.png` | 2026-09-02 (three-times-revised) | **Same three fixes as Script 40, applied here too.** Final re-run: Nashik-hub LOEO AUC onion_2019 0.757, onion_2020 0.785, onion_2023 0.878. Placebo p-values: onion_2019 p=0.125, onion_2020 p=0.396, onion_2023 p=0.396 — **none reach p<0.05**, but the localization hypothesis's directional pattern still holds for the episode it was built to test: onion_2020's Nashik-hub p (0.396) is meaningfully lower than Script 40's corrected national-level p for the same episode. Supersedes every earlier number for this script — the original "0.660→0.064" headline, and this session's two intermediate figures (an initial "→0.000" before the placebo redesign, then "→0.340" after it but before the label-leakage fix). Honest current answer: weak, non-significant, but directionally consistent support for localization. |
| `42_Market_Leader_Network.py` | `table_market_leader_network.csv`, `table_market_leader_ranking.csv`, `table_market_leader_confound_check.csv`, `table_market_leader_stability.csv`, `fig_market_leader_ranking.png`, `fig_market_leader_stability.png` | 2026-08-10 | Extends Script 29's top-5-market lead-lag test to the top-20 markets per crop with a net-leadership-score summary. Clear, geographically coherent leader clusters per crop (Karnataka for tomato, Nashik belt for onion, West Bengal for potato); consumption hubs (Azadpur, Shahdara) are consistent followers across both tomato AND onion independently. Confound check clean for all 3 crops (leadership isn't a reporting-coverage artefact: ρ=0.06-0.37, p>0.10 throughout). Stability check: tomato/potato leaders hold rank across an early/late time-period split; onion's single top-ranked market shuffles rank (Chandvad #2→#5.5 of 16), though the Nashik-belt cluster as a whole stays dominant in both periods — reported as the honest boundary of the finding, not smoothed over. |

## Known gaps not yet reflected anywhere above
- ~~Script 24's dashboard `predict()` train/serve skew~~ — **fixed 2026-09-13** (commit `3d16e45`): `feature_row.get(c, 0)` only substituted 0 for a MISSING key, not a real NaN value; confirmed live via `reference_rows.csv` genuinely carrying NaN in model-feature columns (price/arrival lags, rolling stats, `wage_agri_men`/`women`) for thinner-history markets. Fixed with an explicit `.fillna(0)` matching Script 23's training-time convention. **Real-world impact, per the same measurement done for the web/backend twin of this bug (below): zero** — `model.predict()` on raw NaN vs `fillna(0)` gave identical results across all 6,948 real affected (model, row) combinations, so this never actually served a wrong prediction either; the fix closes a real correctness fragility, not an active data-corruption incident.
- ~~The same class of skew likely also exists in the production React/Node dashboard's inference path~~ — **fixed 2026-09-13** (commit `7772fed`): `toNumeric()` in `web/backend/src/models/index.js` returned `NaN` for a missing/non-numeric value (fed into the m2cgen tree code's native NaN-routing), now returns `0`, matching Script 23's training-time `fillna(0)`. Also fixed the verification suite (`web/generate_js_models.py` built its Python "ground truth" via `model.predict(X)` on X still carrying real NaN, so `verify.mjs` only confirmed JS-native-NaN-routing matched Python's own, never the trained convention) — regenerated all 12 fixtures against corrected ground truth, `verify.mjs` passes all 12 at `max_diff=0.000e+0`. **Real-world impact, measured directly, turned out to be zero**: ran `model.predict()` (raw NaN) vs `model.predict(X.fillna(0))` across every one of 6,948 real (model, row) combinations with a NaN feature (spanning all 12 models, all 1,737 reference rows) — **identical prediction in every single case, max diff 0.0**. LightGBM assigns every split a genuine trained default-direction for missing values, and it happens to coincide exactly with where a literal 0 routes here, plausibly because `fillna(0)` clustered every originally-missing training row at exactly 0 (mechanism plausible, not traced node-by-node). So no live user was ever actually served a wrong prediction from this specific bug, on either dashboard — the fix removes a real correctness fragility (an untested coincidence, not something to rely on across a future retrain) and makes the verification suite test the right thing, but this was not a live-data-corruption incident the way the VEDAS duplicate-polygon bug was. The 12 model `.js` files themselves are unchanged (confirmed via `git diff`) — only input preparation changed, not the trained tree logic.
- Full-capacity TFT run (Script 17) — still deferred, see README §9. ~~Also still carries the market-name collision bug~~ — **fixed 2026-09-13** (commit `db7a005`): `series_id`, the FAST_MODE coverage groupby, and its isin() filter now all key on `market_id`. Validated via SMOKE_TEST (redirected to a scratch dir) — trains through real gradient steps with no error; not run to completion or attempted at full capacity. The reduced-scope run's old numbers (`tft_raw_results.csv`, `table_tft_vs_lgbm.csv`) still predate this fix and remain untrustworthy for the reason already on record (reduced scope), now joined by this one being resolved rather than added to.
- Scripts 12, 13, 15b, 15c, 17, 26 — deferred re-runs, still genuinely open. (18b is **no longer** on this list — re-run 2026-08-15, see its table row above.) These predate not just the 2026-08-01 grid fix but every fix since (policy/PANEL_END, WPI vintage, macro long-history, `s2_ndvi_anom` leakage, the 2026-08-14 market_id collision fix, and the panel's growth from 834/809/82 to 840/814/82 markets).
- ~~Scripts 34/35/36 (rejected two-phase architecture) — never verified against the market_id collision fix~~ — **corrected 2026-09-13**: 35/36 were actually fixed back on 2026-08-20 (commit `cacdea5`, same pass as Scripts 38/39), confirmed by reading the current code (`groupby('market_id')` throughout); 34 never had the bug (no per-market feature engineering). An earlier write-up of this section was stale and said otherwise — see the corrected note above. No longer an open item.
- Daily-resolution forecasting: tried training genuinely new daily models (LightGBM M6) on 2026-07-29 — abandoned, daily naive persistence won even more decisively than weekly naive, and the daily coverage filter collapsed market counts 3-6x. Script 26 (2026-07-30) instead disaggregates the existing weekly model's forecasts into a smooth daily curve with an honest uncertainty band — not a validated daily forecast, a visualization aid. Wired into the dashboard as a "Daily price view" expander below the ticker.
- Arrivals-outcome SDID (Scripts 38b/39b, new 2026-08-21): both robustness checks now done — see the 2026-08-21 note and their table rows above. No longer an open item, listed here only so this section's history stays complete.
- Manuscript: previous manuscript-drafting pass cleared from `paper_drafts/` on 2026-08-21 to start a fresh, section-by-section pass — see README §9 and current chat/session history for status, since this working folder does not persist between passes.
- Two pre-existing mislabeled macro series (`agri_wages_rs_day`, `iip_food_proc`) kept as-is for continuity — see README §9, no commit has addressed this.
- ~~`geocode_cache.json` wrong coordinates for two Tamil Nadu districts~~ — **fixed 2026-09-11**, see Script 16's row above. Also worth recording precisely since the initial write-up (below, when this was found) overstated the severity: the wrong geocode never reached any trained model — `crop_weekly_features.csv` (what Scripts 15/23 actually train on) is a crop-level average across fixed zone definitions, not dependent on market-level geocoding at all; only the informational `zone_assignment.csv` and the unused, gitignored `market_zone_features.csv` were affected. Root cause: Nominatim's top match for the panel's misspelled district names ("Thirunelveli"/"Thirupur" vs OSM's "Tirunelveli"/"Tiruppur") was a random small POI (a bakery, a park) in Chennai, not the district boundary.
- **This session's work (2026-08-14 through 2026-08-21, commit `b8686fa`) was committed and pushed to `origin/master` on 2026-08-21** — prior sessions had left roughly a week of fixes uncommitted locally; confirm this stays current practice going forward rather than accumulating another backlog.
- **M7 (drought) is NOT wired into `23_Train_Production_Models.py`** — investigated 2026-09-14, deliberately deferred, not forgotten. See the 2026-09-14 fold-level follow-up row above (after Script 55's row): a fold-level robustness check found only 4 of Script 15's 5 CV folds carry any real drought signal (fold 1 predates both drought sources entirely), and no crop x horizon cell wins in more than 3 of those 4 folds — not distinguishable from noise. Script 55's per-market significance table looked much stronger but was shown to be inflated by all markets within a fold sharing one fitted model. Revisit once VEDAS/IDM accumulate more years of coverage (more usable folds) or the CV design changes to extract more independent trials.
- **M8 (fertilizer MRP) is NOT wired into `23_Train_Production_Models.py`** — investigated 2026-09-21, deliberately deferred, not forgotten. See the 2026-09-21 fold-level follow-up row above (after Script 60's row): no crop x horizon cell wins in more than 3 of 5 CV folds (binomial p=1.000 even at the best case) — same verdict as M7, for the same structural reason (per-market significance inflated by markets within a fold sharing one fitted model), even though fertilizer MRP has real training data in every fold (unlike M7's fold-1 problem, so this isn't a data-recency issue — the effect is just too small/inconsistent to clear 5 independent trials). CPI-AL/RL (acquired alongside fertilizer) was never tested at all — its training window covers 0-1 months in any fold, an even more severe version of M7's original data-recency problem; revisit once it has 2-3 years of history.
- **M9 (NOAA ONI) is NOT wired into `23_Train_Production_Models.py`** — investigated 2026-09-21, deliberately deferred, not forgotten. See the 2026-09-21 fold-level follow-up row above (after Script 64's row): only 1 of 12 crop x horizon cells (onion h=1w) reaches fold-level significance (5/5 folds, binomial p=0.031) — a real, broad effect, not a single-fold artifact. Every other cell falls short of significance, including potato h=1w, whose per-market table looked the most one-sided (50-0 unanimous) but wins only 3/5 folds — the same pseudo-replication artifact that inflated M7/M8's strongest-looking per-market cells. One significant cell out of twelve isn't enough on its own to justify production wiring, but it's the single strongest fold-level result found across all three new layers (M7, M8, M9) investigated this round — worth prioritizing first if the CV design is ever redesigned for more independent trials. **2026-09-22 update**: a CV-extension experiment (8 folds instead of 5, using otherwise-unused 2017-2021 panel history) confirmed onion h=1w holds at 7/8 folds (p=0.035) rather than diluting under the larger fold set — real added confidence this is a genuine effect, not a 5-fold coincidence. A companion M7 fold-1 boundary fix (recovering the ~15 weeks of real IDM CDI signal fold 1 was missing) left M7's verdict unchanged — its problem was genuinely too-small-an-effect, not the degenerate fold. Neither experiment changes the production-wiring decision on its own; both are documented in the 2026-09-22 rows above for reference if this is revisited.
- **The core M6 production stack has never been shown to beat price-only (M0) at the fold level either** — found 2026-09-21 while investigating M7/M8, not a new problem introduced by them. Only Script 18b's per-market table was ever recorded for M0-vs-M6; running the same fold-level check applied to M7/M8 shows no crop x horizon cell reaching significance in favor of M6, and potato h=26w trends the other way (M0 wins all 5 folds, p=0.062). See the 2026-09-21 ad hoc row above (before Script 61's row). Not treated as proof the layers are worthless — 5 folds is underpowered for anything but a large effect — but it means the project's premise ("more data layers -> better forecasts") is currently unproven by its own evidentiary standard, not just for M7/M8 but for the whole M2-M6 stack. Script 61's diagnostic (row below) is the first concrete follow-up: distinguishing "features aren't used" (true for M7's drought) from "features are used but redundant with existing signal" (true for M8's fertilizer, specifically MOP) is the difference between "wait for more data" and "engineer better features," and changes what the next experiment should be.
